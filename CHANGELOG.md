# Журнал изменений Hillcourt

Владелец файла: Orchestrator (создан по прямому указанию в сессии
«МЕСТНОСТЬ И ПУТЬ», 2026-09-21). Записи — короткие, на русском:
что добавлено, проверка, вердикт.

## История до changelog (восстановлено по ADR, git-истории нет)

Даты ранних ADR в документах не зафиксированы (порядок — по номерам);
все даты 20–21.09.2026 — из текста ADR. Номера 0005 в `docs/decisions/` нет.

- 0001. Bootstrap фазы 0: каркас монорепо (`sim/`, каталоги YAML,
  документы-законы, роли и зоны в `AGENTS.md`). Без чужих движков.
- 0002. Review фазы 0 (Critic): 15/15 тестов, дельта материи ~7e-12,
  4 пункта скоупа подтверждены эмпирически.
- 0003. Legal + Info: режимы земли, правовые пресеты, повинности,
  `Report` с шумом, `Hazard` как популяция с сытостью.
- 0004. Агентная экономика v0 (Economist): потребности, действия двора,
  страх, износ инструмента, локальный обмен.
- 0006. Оси манора (Legal): пресеты, бандлы повинностей, наделы;
  валюты — трудодни, гэфоль, паёк. Yield не тронут.
- 0007. Экономика манора (Economist): трудодень, домен, гэфоль, паёк,
  найм; месячное восстановление труда.
- 0008. Сущность Manor: книга земли, вложенный тэн (depth ≤ 1),
  действия игрока.
- 0009. Вижн-проход Game Director: зафиксировано, чем игра есть/нет;
  код не менялся; 108 тестов зелёные.
- 0010. Контракт амбара тэна: `stock_id`, grant/revoke, голод по своему
  стоку.
- 0011. Ночь Tech Lead, 20–21.09.2026: соль деревня→замок через `Pack`,
  телега и обмен, уход через `Pack`, фикс seed, `player_actions`,
  второй посёлок (деревня у ясеня).
- 0012. Review фаз A/B (Critic): 133 теста OK.
- 0013. Review фикса B (Critic): амбар тэна, `mustered`.
- 0014. Review фазы G (Critic): зомби-уход, стол тэна, ёмкость клетки.
- 0015. Review фаз H/I (Critic): буфер еды, живая телега, гибель воза,
  второй тэн как хозяйство.
- 0016. Смена 2, 21.09.2026: ворота G1–G6, H, I закрыты; baseline —
  соль 15.6, дельта 0.
- 0017. J1/J2 (Critic): корневой держатель не уходит; `ACCEPTANCE.md`,
  `HASHES.txt` объявлен историей и не переписывается.
- 0018. J3/J4 (Critic): общинный доступ (право, не держание), второй тэн
  в `v0_shire`; канон смены 3.
- 0019. Смена 3, 21.09.2026 (Scribe): J1–J4 закрыты; 217 OK; канон смены 3
  (соль 15.6, живых 17/26, голод 272/535).
- 0020. Смена 3b, 21.09.2026 (Scribe): фикс `_board` (паёк по нужде двора);
  242 OK; живых 18/27, голод 284/556; материя бит-в-бит.
- 0021. Тэн как служба: норма комплектов, `mustered`, кольцо, отзыв;
  yield не поднят, третьего тэна нет.
- 0022. Ревью сессии тэна (`0022_review_thegn_service.md`, независимый
  Critic, 2026-09-21). Коллизия номера с ADR пути resolved: ADR пути
  переехал на 0023 (был записан как 0022 до появления ревью).
- Сессия тэна, follow-up без ADR (со слов агента сессии, 2026-09-21):
  явки гейтят норму (`men_held`), ротация вылазок (держатель последним),
  `send_party actor`, вести барону гонцом, синк `docs/03`, +5 зондов;
  проверка со слов агента: `bash sim/run_tests.sh` — 273 OK.

## 2026-09-21 — РЕКА КАК ДОРОГА (сессия воды, ADR 0027)

Добавлено:
- Река в `v0_shire`: клетки `t_03_03`, `t_04_03`, `t_04_04` — вода (`~`),
  брод `fords: [[4, 4]]` на `t_04_04`; берега `t_03_04`/`t_05_04` — суша.
  Остальные сценарии не тронуты; обозы соли/зерна (манхэттен) реку
  не пересекают.
- Благо vessel: `raft`/`boat` (`barn`); рецепты `craft_raft` (брёвна 3.0,
  труд 12) и `craft_boat` (брёвна 4.0 + дрова 0.5, труд 16) в `idle_repair`.
- Профили `water_raft` (вода 1.0) / `water_boat` (вода 0.8), суша им дорога;
  суше вода без брода/моста по-прежнему непроходима.
- `engine/river.py::send_river_pack` (маршрут — `find_path`, риск/разгрузка —
  `resolve_caravans`, `kind="caravan"`): без судна груз > 1.0 не едет;
  в маршруте обязана быть вода. `runner` — handler `send_river`;
  `scenario.py` — секции `fords`/`bridges`/`roads`.
- `sim/tests/test_river_raft.py` — 17 зондов (плот→груз рекой; без плота
  тем же маршрутом — отказ; телега без брода в воду не входит; соль сушей
  жива; дельта 0); `test_march_path.py` — правки под 6 профилей.

Проверка: `bash sim/run_tests.sh` — 362 OK; соль 15.6 на всех трёх мирах,
дельта 0; `v0_shire` 60/1729 — живых 28 (окно 26–28), голод 541.

Не тронуто: служба тэна, скот, вид тайла, yield, живые обозы (манхэттен),
течение (одинаково вверх/вниз), флот.

## 2026-09-21 — СКОТ И ТЯГЛО (сессия тягла, ADR 0025)

Добавлено:
- Каталог: 9 товаров (`ox_m/ox_f`, `donkey_m/donkey_f`, `horse_m/horse_f`,
  молодняк `ox_calf/donkey_foal/horse_foal`); общий `horse` удалён (пол — в id
  стока). Корм — сено (`needs.yml`).
- Рецепты: `breed_*` (пара + 4 сена → молодняк + 3 потерь, шанс 0.08/мес) и
  `mature_*` (молодняк + 2 сена → взрослый + 2 потерь, пол — жребий).
- `sim/src/hillcourt/economy/livestock.py` — месяц стада (корм амбаров/складов,
  приплод, взросление, падёж 0.005/0.01), труд пахоты (вол ×0.7, конь ×0.9),
  тягло воза (осёл ×1.2, конь ×1.3 и дни ×0.75), `can_hold_horse` (только free).
- Пахота (`labor.py`) и воз (`caravan.py`) подключены к тяглу без ломки старых
  сигнатур; `livestock_month` — в конце `phase_consume` (проводка, труд не
  списывает). `tend_animals` выбирается и при тягле.
- `sim/tests/test_draft_livestock.py` — 17 зондов (каталог, пахота, воз,
  приплод, корм, держание коня, служба тэна); `docs/decisions/0025_*` — ADR;
  `docs/09_economy.md` §5/§9/§10 обновлены.

Проверка: `bash sim/run_tests.sh` — 346 OK (17 новых); дельта материи 0 во всех
зондах; канон не двинулся (сценарии не менялись, без скота фаза пуста).

Не тронуто: служба тэна и `mustered` (табун комплектом не считается), каталоги
Legal, реки/вид тайла/радиус усадьбы/yield, `tool_yield_factor` и порядок
none < плуг < лемех, боевое применение коня (флажок чужому агенту).

## 2026-09-21 — МЕСТНОСТЬ И ПУТЬ (сессия пути)

Добавлено:
- `sim/src/hillcourt/engine/terrain.py` — закрытый список `TERRAINS` (9),
  4 профиля хода (`caravan`/`foot`/`hunters`/`mounted`), цены в днях,
  вода без брода/моста непроходима.
- `sim/src/hillcourt/engine/path.py` — `find_path` (Дейкстра, список клеток),
  туман через `known`/`memory_roads`, `known_tiles` только из `Report`.
- `sim/src/hillcourt/engine/march.py` — приказ `send_march`
  (`Pack kind="party"`, в логе `route`/`months`/`profile`).
- `sim/tests/test_march_path.py` — 15 зондов (обход чащи обозом 6.0 дн.,
  проход охотников 4.5 дн., брод, туман, лог приказа).
- `docs/decisions/0023_march_path.md` — ADR сессии.

Изменено:
- `sim/src/hillcourt/ontology.py` — `Tile` +`road`/`ford`/`bridge`
  (default `False`, обратно совместимо).
- `sim/src/hillcourt/runner.py` — только handler `send_march`.

Проверка: `bash sim/run_tests.sh` — 268 OK; канон 3b держится
(соль 15.6, дельта 0, живых/голод 12/33, 18/284, 27/556).
Критик (субагент): ПРИНЯТО С ЗАМЕЧАНИЯМИ, 6 миноров, блокеров нет.

Не тронуто: обозы (манхэттен), `send_party`, служба тэна, календарь,
каталоги, сценарии, `docs/03`.

Остаток для владельцев: Scribe — синхронизировать `docs/03_ontology.md`
(поля `Tile`, действие `send_march` в `PlayerAction`).

## 2026-09-21 — НАПОЛНЕНИЕ ТАЙЛА (вид клетки)

Добавлено:
- `sim/src/hillcourt/engine/tile_view.py` — кап жилых дворов 5
  (`TILE_MAX_HOUSEHOLDS`, эквивалент `tile.max_households`), посадка
  `settle_household` (сверх капа — `PermissionError` + `settle_rejected`,
  шестой не садится), каталог из 7 форм без города, снимок `tile_view`
  (`form`, `household_count`, `has_seat`, `has_thegn_hall`, `terrain`),
  дамп `dump_tile_views`/`format_tile_view` (вид без RimWorld-слоя).
- `sim/tests/test_tile_view.py` — 13 зондов (кап, формы пусто/1/4/seat,
  legacy-переполнение → `village`, чистота снимка, тик дельта 0).
- `docs/decisions/0024_tile_view.md` — ADR сессии + пример дампа.

Изменено:
- `sim/src/hillcourt/engine/tick.py` — `phase_migrate` не выбирает полную
  клетку (некуда — остаётся).
- `sim/src/hillcourt/hazards/travel.py` — прибытие на полную клетку
  возвращается к исходной (`settle_rejected_arrival`, груз цел).
- `sim/src/hillcourt/runner.py` — `--print-log` печатает вид всех клеток.

Проверка: `bash sim/run_tests.sh` — 296 OK; дельта материи 0.
Ролевой разбор (4 субагента): игрок, житель, тэн, путник — учтены.

Не тронуто: служба тэна, реки, скот, yield, барщина, pathfinder,
каталоги Economist/Legal, сценарии, `docs/03`, спрайты/`client/`.

## 2026-09-21 — ТАЙЛЫ ЛАГЕРЕЙ, ПЕРЕПРАВЫ, ПОЛЯ ГОСПОДИНА (виды)

Добавлено:
- `engine/tile_view.py`: каталог 7 → 17. `lord_field` (чистый деменский
  клин), `crossing` (брод/мост; пристань плотов и лодок), `bog_iron`
  (пустая топь; `waste` теперь без топи), 6 лагерей из возов в пути
  (`foot_pot_camp` ≤2, `campfire_camp` =3, `tent_camp` переселенцы,
  `pavilion_camp` шатёр тэна/держателя, `wagon_camp` обоз,
  `fortified_camp` ≥4), `quarry` резервом (камня в экономике нет).
- Приоритет: зал > усадьба > переправа > лагерь > поле господина > счётчик.
  Лес без новых тайлов (промыслы не тайловые, как охота).
- `sim/tests/test_tile_camps.py` — 16 зондов.
- `docs/decisions/0026_tile_icons_camps_crossing_lord.md` — ADR.

Проверка: `bash sim/run_tests.sh` — 362 OK; дельта 0; каноны соли/зерна те же.

Не тронуто: служба тэна, реки как механика, скот, yield, барщина, pathfinder,
каталоги, сценарии, `docs/03`, спрайты/`client/`.

## 2026-09-21 — СТРОЙКА ПУТИ (дорога, брод, мост проектом манора)

Добавлено:
- `sim/src/hillcourt/engine/roadworks.py` — стройка как проект барщины:
  `start_work` (`work_road`/`work_ford`/`work_bridge`; корень только своя
  книга, клетка тэна — `PermissionError`), материал `log` (4/2/6)
  переводом в `sink:waste`, труд из остатка рук после барщины до поля
  (до 12 дней/мес; дорога 30, брод 20, мост 40), завершение поднимает tag
  клетки один раз (`road`/`ford`/`bridge`), лог `work_done`.
- `sim/src/hillcourt/engine/tick.py` — фаза `phase_roadworks` после
  `phase_manor`, до `phase_labor` (без работ — no-op).
- `sim/src/hillcourt/world.py` — поле `roadworks` + условные части хеша
  (`way|`/`roadwork|` только при непустом; пустые работы канон не двигают).
- `sim/src/hillcourt/runner.py` — handlers `work_road`/`work_ford`/`work_bridge`.
- `sim/tests/test_roadworks.py` — 16 зондов (лог и старый cost/tag до
  завершения; дорога 8.0→0.8 у `caravan`, дни `find_path` падают; брод
  открывает воду; мост ставит `bridge`; без рук нет завершения; поле теряет
  руки; материя 0; соль `caravan_load`/`caravan_unload` жива; кап 5 цел).
- `docs/decisions/0025_roadworks_way_project.md` — ADR стыка с path/water.

Проверка: `PYTHONPATH=sim/src python3 -m unittest sim.tests.test_roadworks` —
16 OK; `v0_hill_and_salt` 36 мес seed 1729 — соль 15.6, дельта −0.000000,
живых 12, голод 33.
Ролевой разбор (субагент, Muse Spark 1.3 Free): держатель/виллан/обоз/шериф —
учтены в ADR. Критик (субагент, Muse Spark 1.3 Free): ПРИНЯТО, блокеров нет.

Не тронуто: `terrain.py`/`path.py`/`river.py` (только чтение tag),
служба тэна, скот, вид тайла (кап 5), yield/seasons, каталоги, сценарии,
`docs/03`, `client/`.

## 2026-09-21 — ИНТЕГРАЦИЯ ЗАКОНА (сводная, ADR 0028)

Сшивка шести сдач (0021–0027) в один закон, без нового кода.

Вшито в существо документов:
- `docs/03`: `Tile.road/ford/bridge` + `standing_stock_id`; `Pack.kind` реальный
  (`pack/party/caravan/household_move`); `Manor.seat_*`/`service_*`; реестр
  `World.roadworks`, `TILE_MAX_HOUSEHOLDS=5`; `PlayerAction` по домам
  (`send_march/send_sally/send_river/work_*`; `settle_household` — правило места, не приказ).
- `docs/04`: фаз стало **16** — вклинилась `phase_roadworks`; `thegn_month`,
  `livestock_month`, `peace_theft_factor`, глаз зала в `phase_inform`,
  оговорка про манхэттен живого обоза; `state_hash` расширен.
- `docs/05`: глаз холма — радиус 1 от зала, `grain_approx`, без `grain`; даль
  только `caravan`/`silence`.
- `docs/06`: соляная деревня — живой `Pack`, не «только Report».
- `docs/07`/`08`/`09_law`: служба тэна (комплект, `guard_ring`, отзыв →
  `free_landless`), снято «в работе», раздел «Стыки».
- `docs/02`: вода/стройка/скот/кап/место — в «Входит»; манхэттен живого обоза,
  течение, флот, боевой конь, камень — в «Отложено».
- `design/scenarios/ACCEPTANCE.md`: §7 — качественные окна интеграции.

Имена схлопнуты: `Pack.kind` (убран `messenger`/`settlers`), общий `horse`
убран; «радиус манора» разведён на глаз холма и кольцо тэна.

Проверка: `bash sim/run_tests.sh` — 362 OK. Канон держится (ревизия, не
подгонка): hill 36/1729 — соль 15.6, дельта `-0.000000`, живых 12, голод 33;
two 60/42 — соль 15.6, дельта `-0.000000`, живых 18 (окно 17–19), голод 284;
shire 60/1729 — соль 15.6, дельта `-0.000000`, живых 28 (окно 26–28), голод 541.

Роли стола (держатель, тэн, виллан ближний/дальний, ездовой/плотовщик, шериф,
скотник) — **ПРИНЯТО**; независимый Critic — **ПРИНЯТО С ЗАМЕЧАНИЯМИ** (дефекты
только И-9 «закон↔код», инварианты чисты). По итогам правки Scribe внесены:
`grain_approx` — зерно, не число дворов; `mustered` (комплект на дворе) vs
`service_met` (`fief_kits` = амбар+двор); «не краской» смягчено (стартовая карта
сценария — исключение); `settle_household` — правило места, не приказ runner;
`Tile.standing_stock_id`; `water_*` дорого, но идут сушей; `docs/09_economy` —
16 фаз; ACCEPTANCE — «шестой не садится». Целевые зонды — 89 OK.

Не тронуто: код `sim/`, каталоги, сценарии, `client/`. Ещё красные зонды (GAP):
живой обоз не читает дорогу; `form` не в законе для игрока; течение/флот;
боевой конь; камень/`quarry`; спавн `Hazard`/руин; дневной контур.

## 2026-09-21 — ЖИВОЙ ВОЗ ЧИТАЕТ ПУТЬ (цикл A, ADR 0029)

Столп «дорога меняет ход `Pack`» закрыт для живого обоза, а не только для приказов.

Код (`economy/caravan.py`, зона Economist):
- `_dispatch_rule` строит маршрут `engine/path.py::find_path(..., "caravan")`
  (`known=None`: возницы землю знают, тумана игрока нет); `None` — воз не идёт.
- `Pack.route` — маршрут `find_path` (включает origin, как `send_march`); дни — сумма
  дней входа; прежние множители телеги/тягла сохранены; фураж и риск — по входам
  (`_route_entries`, `len(route)-1`); `_manhattan_route` удалён.
- Материя — только `Ledger.transfer`, дельта 0, нового RNG нет.

Проверка: `bash sim/run_tests.sh` — **364 OK** (+2 зонда). Канон (окна те же, хеш —
снимок ревизии): hill 36/1729 — соль 15.6, дельта `-0.000000`, живых 12, голод 33,
hash `4124a48b486f`; two 60/42 — живых 18, голод 284, hash `6994c4cfab8e`; shire
60/1729 — живых 28, голод 541, hash `602287af84ff`. Маршруты: соль 11/11→11/20 (hill)
и 15/19 (two, shire); зерно 7/7→7/14, новый путь обходит топь `t_02_04`. `eta` в
месяцах не сдвинулась — каденция/окна соли те же.

Закон: `docs/02` (GAP снят), `docs/03` (`Pack.route` — `find_path`), `docs/04` (фаза
`phase_caravan`), `docs/09_economy` §6/§10, `ACCEPTANCE` §6 (смена 4) и §7.

Срезка GAP: «живой обоз не читает дорогу» — закрыт. Остальные GAP прежние.

## 2026-09-21 — СКОТ И ЖЕЛЕЗО ИЗ HOLLOW В ЖИВЫЕ (хендофф, ADR 0030)

Аудит hollow-систем закрыт механикой, канон удержан.

Economist (каталог + `economy/`): рецепт `smelt_iron_bloom` (`iron 1.2 + firewood 0.6 →
iron_bloom 1.0`, труд 10, без проводки к действию); `can_hold_horse` — механизм
(конь только `free`, `tied` бонуса нет); `gather_draft_hay` из остатка труда;
дропы с причинами — проводка выплавки, плуг, спавн скота, крафт `iron_share`.

Implementer (только `v0_shire.yml` + тест), финал: волы `hh_11`-виллан (там вяжет труд),
ослы `hh_14`, кони `hh_09` (конь у свободного); сено 42.4/17/19 + базовые 5.0 `hh_10`;
`hh_12`/`hh_13` чистые. Вне караванных поселений.

Проверка: `bash sim/run_tests.sh` — **385 OK**. Канон: hill/two **бит-в-бит**;
shire — живых 28, соль 15.6, голод 543, дельта 0, hash `42f140308c90` (новая ревизия,
не подгонка; промежуточная `5d6728d9`/542 — внутри цикла). Леджер соли бит-в-бит.

Закон: `docs/09_economy.md` §9/§10, `docs/02` (входит/отложено), `ACCEPTANCE` (смена 5,
§7). Честные дыры: стадо гибнет к 24 мес; выплавка не подключена; плуг недостижим.

Ревизии H: Скотник — ПРИНЯТО (ADR: причина падежа — остаток труда); Виллан — ПРОТИВ →
закрыто: волы у виллана `hh_11` (зонд пользы зелёный), критерий пар — анти-голод
(`starved` = 0, негативный контроль FAIL), `hh_10` — базовые 5.0; Critic — ПРИНЯТО
С ЗАМЕЧАНИЯМИ (B1 `docs/07` исправлен; B2 кап косьбы закрыт кодом — 382 OK, хеши
бит-в-бит; износ — исключение в законе); Economist-валидатор — ПРИНЯТО (дыры: порядок
«корм→косьба» в том же месяце, плуг множит сено, латентное двойное включение
`gather_hay` — всё в ADR как GAP следующих заходов).

B2 закрыт кодом: `gather_draft_hay` через общий кап клетки (тот же кап 4),
**382 OK**, три хеша бит-в-бит (проверено столом: зонды + шир `5d6728d91362`); +зонды
«общий кап» и «скот не из `external_in`». Исключения в законе: набор `hay_pastures`,
отдельный счётчик фазы, износ только `work_month`.

## 2026-09-22 — E-ЦИКЛ ПРИЗЕМЛЁН; DEMOGRAPHY ЛОМАЕТ ОКНА (ADR 0032)

Постановка E закрыта: стартовый `log: 2.0` (`hh_04`, плуг достижим рецептом, автономно
не строится); сено шире — свиная пара `hh_12` + пастбищные `works_tiles` (тягловую
нельзя: реролит общий RNG трио — доказано); замеры честно отрицательные (дорога
расписанию не окупилась, плот медленнее); `smith_iron_share` официален владельцем
(тест отсутствия → тест присутствия); `phase_hay` (косьба до поля, Task A).

Сюит 411: зелёных 408, красных 3 — ВСЕ демографические (ADR 0031, чужой цикл):
two живых 14 (окно 17–19), шир 21 (окно 24–28); соль 15.6 и дельта 0 держатся.
Доказано in-memory (роды выкл → живые в окнах) + A/B (`phase_hay` невиновен).
Решение стола: окна стоят, автор демографии чинит (ап + окна или урезка) новым ADR;
откатов невиновных нет. Закон: `docs/04` (18 фаз), `docs/09`, `docs/02`,
`ACCEPTANCE` (смена 6 КРАСНО + §7).

Ревизии E: Скотник — ПРОТИВ (двойной вызов косьбы; закон описывает оба честно,
удаление из `livestock_month` предложено владельцу); Economist-валидатор — ПРИНЯТО;
Critic — ПРИНЯТО С ЗАМЕЧАНИЯМИ (блокеров нет; поправлена формулировка про рерол соли,
зафиксированы stale-комменты и авторитет окон — всё в ADR 0032).

## 2026-09-22 — ПРИЁМКА ЗЕЛЁНАЯ: viability E1–E8 + offices (ADR 0033–0036)

Автор довёл: резерв косьбы, гейты дорогого/приплода, покупка (бревно/скот/свиньи),
кап пула, смертность по возрасту + иммунитет расцвета, характер из хеша (жребий цел);
`smith_iron_share` официален (владелец применил). Поле `produces_report` упразднено
(ADR 0034, имплементер: yml/загрузчик/схема/тест).

Проверка столом: `bash sim/run_tests.sh` — **416 OK**; hill 12/15.6/32, two 15/15.6/312,
shire 25/15.6/541; дельта 0 везде. Дрейф живых честно зафиксирован (окна §1 держатся;
узкое 17–19 two — за автором демографии).

Закон: `docs/03` (`Office` без поля), `docs/07` (должность без депеш — включая критерий),
`docs/09` (E1–E8), `docs/02` (пороги 8/30, хеш-характер, лемех подключён), `ACCEPTANCE`
(смена 7 ЗЕЛЁНАЯ + §7), ADR 0036. Долги: проводка выплавки, покупка автором, TBD в
ADR 0035 — финализировать владельцу; `test_offices_produce_reports` — переименовать
следующему имплементеру.

Ревизии приёмки: Держатель — ПРИНЯТО; Critic — ПРИНЯТО С ЗАМЕЧАНИЯМИ (зелень честная,
подгонки нет; синхрон-дотяжки внесены столом).

## 2026-09-23 — ПРИЁМКА SMELT + ID ДВОРА, ДОПУСКИ (ADR 0037–0038)

Вшито столом:
- Проводка `smelt_iron_bloom` засвидетельствована (Economist, ADR 0037): зонды-причины,
  2 строки `docs/09`, нового кода тика — ноль; железо распылено, в шире 30 проводок.
- `messenger`-отчёт об уходе несёт `household_id` + клетку (Info): новых каналов/задержек
  нет, И-3 цел (вести с `confidence 0.5`, не `Tile.state`).

Проверка командой Table: `bash sim/run_tests.sh` — **420 OK**; hill 12/15.6/32,
two 15/15.6/312 (окно 14–18), shire 25/15.6/541 (окно 24–28); дельта 0 везде;
соль только Pack-цепочкой (`load 7/unload 5/11.6`, `external_in 0`, телепорт 0).
Critic — ПРИНЯТО С ЗАМЕЧАНИЯМИ по обеим (дыры поименно, блокеров нет).

Открыто: ADR 0038 — допуски производительности (точечно, через ADR с окнами, где врут
руки); И-1/И-2/И-3 абсолютны; видение баронства — этапы, не закон v0.

Долги: `rejected_tile_ambiguity` (`facts['tile']` = клетка рассказа — докнуть, пины
`lost`/`rejected` добрать); Scribe-пачка (`docs/09 §9` → 0037, `08:63`, `07/08 grant_tool`,
`docs/05:17–18`, имя теста должностей); `hazards/` закреплены за Implementer в `AGENTS.md`.

## 2026-09-23 — ДОПУСК №1 ВШИТ: цена стройки (ADR 0039)

Economist новым ADR: дорога 20дн+3 бревна, брод 14+2, мост 28+4, кап 20/мес
(было 30/20/40, 4/2/6, кап 12). Душило ложное горло — кап, не труд.
Critic — ПРИНЯТО С ЗАМЕЧАНИЯМИ (точечно цене; полномочие: именное исключение без
прецедента, карточка Economist дополнена запретом `engine/`).
Проверка Table: `bash sim/run_tests.sh` — **422 OK**; каноны бит-в-бит
(hill 12/15.6/32, two 15/15.6/312, shire 25/15.6/541), дельта 0; торговая граница
держится (eta 1→1). Scribe-топ-ап чисел в `docs/04`/`docs/02` — следом.

## 2026-09-23 — ДОПУСК №2 ВШИТ: сентябрьский пик (ADR 0040)

Economist: сентябрь `plot_yield` 1.30→**1.60**, демена целы; ловушка августа/зимы измерена
и отклонена (пины нетронутых в тесте). Critic — ПРИНЯТО С ЗАМЕЧАНИЯМИ (точечно, направление
ловушки подтверждено, зона строго своя).
Проверка Table: `bash sim/run_tests.sh` — **424 OK** (плавание 422↔424 закрыто: гонка
с правками в полёте); hill 12/15.6/31, two **14**/15.6/341 (кромка окна 14–18, триггер
перебазировки не сработал), shire 26/15.6/547; дельта 0; соль Pack-цепочкой.
Предзарегистрировано: просадка two до 13 — открытое решение о перебазировке.

## 2026-09-23 — ДОПУСК №3 ВШИТ: фураж по честным дням (ADR 0041)

Economist: `_fodder_amount` по дням `_route_days` (было по входам); речной воз конечен;
риск/eta/каденция/буфер/память не тронуты. Critic — ПРИНЯТО С ЗАМЕЧАНИЯМИ
(гейт two ≥ 14 удержан, монотонность и река просмотрены, зона строго своя).
Проверка Table: `bash sim/run_tests.sh` — **427 OK** стабильно; hill бит-в-бит
(`a5ffefe13285`), two 14/15.6/341 (триггер 13 не сработал), shire 26/15.6/547;
дельта 0; соль Pack-цепочкой, фураж переводом в `sink:eaten`.

## 2026-09-23 — ДОПУСК №4 ВШИТ: контракт `wolves_den` (ADR 0042)

Economist: топ-ап существующих волков (потолки 1.0/6.0, новых Hazard нет,
`report_required: true`); фаза не вшита, молчание структурно + замер 12 мес.
Critic — ПРИНЯТО (вести ниоткуда нет, потолки с обеих границ, зона своя).
Проверка Table: `bash sim/run_tests.sh` — **429 OK**; hill 12/15.6/31, two 14/15.6/341
(гейт ≥ 14), shire 26/15.6/547; хеши бит-в-бит; волки 0.6/0.6, угроз 2→2.

## 2026-09-23 — СВЯЗКА ВШИТА: живые волки (ADR 0042/0043/0044)

Правило (Economist) + фаза (Implementer) + весть (Info) + проводка: топ-ап волков
с потолками через `rng.hazard`, новых Hazard нет; весть `messenger` о каждом росте
(`population_approx`, `tile` = about, дедуп); вызов в `phase_inform`.
Critic — ПРИНЯТО С ЗАМЕЧАНИЯМИ (сдвиг честен, обоз читает только вести).
Проверка Table: `bash sim/run_tests.sh` — **441 OK**; hill 12/15.6/31, two 14/15.6/341
(гейт ≥ 14), shire 25/15.6/539 (окно 24–28); угрозе без вести нет (шир 6/6, two 0/0).

## 2026-09-23 — ШАГ 1 ВАТАГИ ВШИТ: контракт `band_camp` (ADR 0045)

Economist: условное появление стоячей ватаги (`mode: spawn` — дотягивать некого;
гейт `marsh` + ≥3 дворов, не стреляет нигде — запинено по трём мирам; рождение 1.0/3.0,
потолки 1.0/6.0, `rng.hazard`, `report_required: true`; кража 2.0 цела); фазы нет,
молчание структурно + замером. Critic — ПРИНЯТО (покрытие контрактное, форс-ватага
проверена; наказы фазе: рождение только с вестью структурно, гейт per-tick).
Проверка Table: `bash sim/run_tests.sh` — **444 OK**; hill 12/15.6/31, two 14/15.6/341
(гейт ≥ 14), shire 25/15.6/539; угроз 2→2, ватаг 0, вестей про ватагу 0.

## 2026-09-23 — СВЯЗКА ВАТАГИ ВШИТА (ADR 0045/0046 + весть)

Правило (Economist) + фаза (Implementer) + весть (Info): условное рождение стоячей
ватаги (гейт per-tick, `rng.hazard`, потолки, одна на клетку, честный `spawn_rule_id`);
рождение только с вестью — структурно (без читателя — откат, доказано Critic);
весть `messenger` (`population_approx`, about, дедуп). Critic — ПРИНЯТО
(per-tick гейт, кража по своей клетке, связка живая).
Проверка Table: `bash sim/run_tests.sh` — **455 OK**; hill 12/15.6/31, two 14/15.6/341
(гейт ≥ 14), shire 25/15.6/539; ватаг 0/вестей 0 в каноне, форс-связка 7/7;
id после отката переиспользуется без живых дублей — коллизии нет.

## 2026-09-23 — ЧИСТКА ВШИТА: `travel_delay_months` удалён (ADR 0047)

Economist: мёртвый параметр убран из `hazards.yml` (чтений было 0); рычаг (+1 день)
отклонён расчётом — слой ради сотых, отказ по бюджету. Critic — ПРИНЯТО
С ЗАМЕЧАНИЯМИ (tombstone 0046→0047 — закрыто строкой). Проверка Table:
`bash sim/run_tests.sh` — **455 OK**; каноны стоят (12/31, 14/341, 25/539).

## 2026-09-24 — ТРОПЫ И ОБЩИНА ВШИТЫ (ADR 0061/0060)

- **Тропы от ходьбы** (Implementer, ADR 0061): `Tile.trail_wear`, пороги 3/12
  (тропа → грунтовка), затухание 0.5/мес, вес на воз 4.0 / отряд 2.0 / ходок 1.0 / река 0,
  профили-строки `arms`/`rider`, множители 0.9/0.8 в `find_path`, топтание в тике,
  память грунта для приказов, вести о новой тропе (Info). Скидка: соль 19.0 → 18.6 дн —
  быстрее, но не до нуля; вода и строеная дорога вне системы; вдали — молчание.
- **Община как право** (Economist, ADR 0060): `Right.kind=common` исполняем — общий
  доступ, **не** индивидуальное держание (`own_tiles`/`_held_tile_ids` исключают);
  `grazing` остаётся личным наделом.
- Critic: тропы ПРИНЯТО (без дыр), `common` ПРИНЯТО (замечание по `grazing` учтено в
  ADR 0062). Проверка Table: `bash sim/run_tests.sh` — **505 OK**; hill 12/15.6/29,
  two 14/15.6/341 (гейт ≥ 14), shire 24/15.6/530 (кромка 24–28); дельта 0; тропа не
  раскрывает карту (И-3 цел).

## 2026-09-24 — ПЛЕМЯ v1 ВШИТО: данные, вид, загрузчик (ADR 0059/0060/0062/0064)

Племя — независимый агент на земле барона: `Settlement.kind=native_village` (данные, не
скрытый маркер), форма `tribal_village` выводится из вида поселения, дворы `free_landless`
вне книги корня, общая земля — `Right.kind=common` (общий доступ, не держание) + `works_tiles`;
`grazing` остаётся личным наделом. Племя не «просто уходит» — guard в `phase_migrate`.
`Tribe` урезан Systems-гейтом до 7 полей (`id, name, stance, settlement_id, tribute_grain,
muster_kits`); `loyalty`/`muster_profile`/`own_economy`/`granted_land`/`annexed` — v2.
Critic — ПРИНЯТО (биекция, И-3 цел: `known_tiles` не растёт, бонусов/RNG нет).
Проверка Table: **505 OK**; hill 12/15.6/29, two 14/15.6/341, shire 24/15.6/530; дельта 0.

## 2026-09-23 — ТОРГ ВШИТ: соседский кластер (ADR 0057)

Economist: клетка + 4 ортогональных (носитель — двор, Pack нет); те же товары/гейты/
фикс-цены; цена носителя (сбор 0.5 где сыт, укус 0.25 с богатой стороны, кап зерна 2.0,
дар без цены); только `transfer`, бросков нет (дар 0.3 предшествует — текстом в ADR).
Пин нейтральности заменён механизмом цены. Critic — ПРИНЯТО С ЗАМЕЧАНИЯМИ.
Проверка Table: `bash sim/run_tests.sh` — **479 OK**; hill 12/15.6/29, two 14/15.6/341
(гейт ≥ 14), shire 24/15.6/530 (кромка 24–28); соль только обозом, дальний торг — обозом.

## 2026-09-23 — ВЫПАС ВШИТ: сено зимой, трава с краем (ADR 0053/0055/0056)

Economist: летом 30% сено + 70% выпас со стоячего сена пастбищ (кап 2.0/клетку/мес,
порядок по id, проводка `graze`, недобор → сено); зима — полное сено; дворовый корм —
по именному разрешению (только норма, без прецедента); бесплатной траве — отказ.
Единицы стенда — errata 0056 (за 60 мес). Critic — ПРИНЯТО С ЗАМЕЧАНИЯМИ.
Проверка Table: `bash sim/run_tests.sh` — **478 OK**; hill 12/15.6/31, two 14/15.6/341
(гейт ≥ 14), shire 24/15.6/489; `animals_starved` 7.0/60 мес, `hay_grazed` 75.3 —
середина между 10 и 0.

## 2026-09-23 — ЗЕМЛЯ ВШИТА: вид и данные (ADR 0054)

World: FORMS 17→26, `LAND_MARKS` без полей на `Tile`, стадии роста без городов
(`large_village`), ёмкость выпаса + `mine_sites`, скетч баронства; механики —
только предложкой хозяину. Critic — ПРИНЯТО (вид без бонусов, тик не задет, И-5/И-8).
Проверка Table: `bash sim/run_tests.sh` — **476 OK**; hill 12/15.6/31, two 14/15.6/341
(гейт ≥ 14), shire 24/15.6/489; детерминизм повтором; роль World — в `AGENTS.md`.

## 2026-09-23 — ПАКЕТ ВШИТ: пин на механизм + маржа трио (ADR 0051/0052)

Жребий стойла изолирован (хеш-поток, детерминизм, дельта 0); красная метка разобрана
судом Critic (системная тонкость запаса, не удача); пин делён (нейтральность механизмом,
выживание маржой); доливка сена с запасом и пином конечности; таблица стенда исправлена
(15 поименно + 25, удачная выборка признана). Critic — ПРИНЯТО С ЗАМЕЧАНИЯМИ.
Проверка Table: `bash sim/run_tests.sh` — **458 OK**; hill 12/15.6/31, two 14/15.6/341
(гейт ≥ 14), shire 24/15.6/489 (кромка 24–28); пары `starved` = 0.

## 2026-09-23 — ОДНА КОСЬБА ВШИТА (ADR 0050)

Economist: добор из `livestock_month` убран, `_mow_reserve` удалена — единственная
косьба `phase_hay` (добор косил 0 за 60 мес, резерв жал поле зря). Critic — ПРИНЯТО
С ЗАМЕЧАНИЯМИ (механизм + следствия, ссылка 0049→0050 закрыта строкой).
Проверка Table: `bash sim/run_tests.sh` — **455 OK**; hill/two бит-в-бит
(12/31, 14/341 — гейт ≥ 14), shire 26/15.6/545; пары `starved` = 0, сено — 378 проводок
ранней фазой.

## 2026-09-23 — УЗКОЕ ОКНО ЗАКРЫТО (ADR 0048)

Автор демографии: узкое two 17–19 снято решением, авторитет — §1 (14–18). A/B:
роды вкл — 14/341/15 ртов, выкл — 17/288/0; разница — рты→голод→уход. Подъём отдачи —
гонка без сходимости, урезка — убийство ядра. Кода нет, пороги целы.
Critic — ПРИНЯТО (A/B воспроизведён направлением, OFF — до цифры).

## 2026-09-23 — МИКРО-ХВОСТЫ ВШИТЫ (имена и гейты, поведения ноль)

Implementer: `test_offices_produce_reports` → `test_offices_travel_no_reports` (тело то же);
явный `depth`-гейт в `grant_thegn` (`reason=depth`); docstring про `men_held`;
метка `scenario.py:271` → `None`. Critic — ПРИНЯТО без замечаний (гейт своим прогоном:
вложенный — отказ без новых маноров; hill бит-в-бит `a5ffefe13285`).

Роли A (обозник, держатель, шериф) — **ПРИНЯТО**; Economist-валидатор — **ПРИНЯТО**;
Critic — **ПРИНЯТО С ЗАМЕЧАНИЯМИ** (блокеры И-9 закрыты правкой: `docs/09` без
манхэттена, `docs/03` route с речной оговоркой, `docs/07` без `send_party` в задержке,
`docs/04` квантование `max(1, ceil)` + память дорог + памятка стройки, ADR 0029
дополнен ревизией). Честный итог: маршрут живого обоза от `find_path`, но окупаемость
стройки для торга в v0 нулевая (все пути < 30 дней) — следующая приёмка обязана измерять
возврат, а не длину пути. Долг: `river.py:252` (мёртвая ссылка) — следующему
имплементеру.

## 2026-09-24 — ГЕКСЫ И ЧЕСТНОЕ ВРЕМЯ ВШИТЫ (ADR 0071/0076/0077/0078)

Implementer: шестигранная сетка — `engine/hexgrid.py` единственный источник соседства
(6 направлений, offset↔axial, расстояние); `state_hash` покрывает координаты; чужие зоны
(`decisions`, `labor`, `livestock`, `exchange`, `thegn`, `regimes`) — только импорт хелпера.
Честное время: `travel_hours` (нога 0,65 / конь 0,40 / обоз 0,85 ч на гекс, `arms` ×1,25),
`entry_hours = entry_cost × hours_scale`, `eta_date` по суткам (месяц = 30 суток), дневной
контур `phase_day` только для `Pack` — месяц остался основным тиком (И-4 цел); 12 пинов
перенесены на измеренные часы, новый `test_travel_hours.py` (20 тестов). Legal (ADR 0077):
соседство не даёт прав — communal-доступ только `works_tiles`/`Right.common`; Economist
вычистил ложные докстринги своей зоны.

Critic — **ПРИНЯТО С ЗАМЕЧАНИЯМИ** (И-4 цел, скрытых множителей нет, 42/42 профильных,
дельты/детерминизм ок; замечание существенное: соляный канал отчётов пуст — 0/39 ненулевых,
обоз увозит соль до `phase_inform`). Table — **ПРИНЯТО**, починок 0, freeze соблюдён
(последняя правка 19:18:44, приёмка 19:29–19:48): `bash sim/run_tests.sh` — **591 OK**;
hill 13/15.6/28, two 17/15.6/383 (окно 14–18), shire 25/15.6/535 (окно 24–28), соль 15.6,
дельта 0, повтор шира — тот же хеш; глазами: 100 гексов пешком 2–3 суток, всадник быстрее
пешего, брод/плот имеют смысл, выпас 5–8, сено до пашни, пастбище по праву.

**Дыра в работе (ADR 0079, следующий срез):** отчёт — события месяца, не снимок склада;
честный детектор вместо удалённого пина. Правило кандидата миграции — ADR 0080.

## 2026-09-24 — ПАКЕТ ИЗ ПЯТИ СРЕЗОВ ВШИТ (соль, вода, карта 100×100, вид, служба)

Соль (ADR 0079/0081): отчёт строится из `world.month_events` (проводки месяца), а не снимка
склада; атрибуция по владельцу стока (дворы, не поселение); отток виден; `salt_approx`/
`salt_received` не пустые. **Дыра 0/39 закрыта:** ненулевые соляные отчёты — Table: hill 4/4,
two 11/11, shire 10/10; Critic при иной методике подсчёта: 2/9, 3/15, 1/16 — обе проверки
подтверждают живый канал и отсутствие вранья.
Вода v1 (ADR 0075): ёмкость 6/12, люди 2/4 не бесплатны, фураж по долям суток (рейс ≤6 ч без
фуража), портовый сбор 10 % натурой, соль exempt, всё `transfer` — материя не создаётся.
Карта 100×100 (ADR 0073): 10 000 гексов, река 6-связна (199 водных гексов, 3 брода, 2 моста,
порт = поселение + marker), 12 поселений, 600 дворов (город 5×20 + 3 резерва, 8 деревень
8×50), леса 400/360/480, wilds 8 283; рост ленивый, O(поселения) — 52× гексов ≈ 3,5× времени.
Служба (ADR 0082): повинность `muster` 2/3 мес, outbound↔return, `call_status`
(`in_service/returned/overdue`), гибель = `unable`, тишина ≠ отказ, подавление тэна через
`revoke_thegn`, регент/`annexed` вне v1. Вид: `CoarseMapView` — только чтение данных, без
выдачи знания (И-3).

Critic: **А/Б/Д ПРИНЯТО, В/E ПРИНЯТО С ЗАМЕЧАНИЕМ** (рост не подключён к тику — карта принята
как данные; `state_hash` не покрывает `call_status`/`obligation_id` — замер подтвердил
одинаковый хеш у разных состояний, обязательный follow-up). Table: **ПРИНЯТО**,
`Ran 636 tests … OK`, правок 0, freeze соблюдён. Каноны: hill 13/15.6/28 `79796030ead0`,
two 17/15.6/383 `7fddcf96722c` (окно 14–18), shire 25/15.6/535 `73f56b9133d6` (окно 24–28),
соль 15.6, дельта 0, повтор хеша — тот же. Новая карта: 12 мес, 550 живых, дельта 0, хеш
`3f8d173ce5d7`.

Следующий срез (в работе): подключение роста к тику, покрытие `state_hash`, ADR 0080
(пригодный гекс) и **ADR 0083** (механический кап: сельский 5, `urban` 20 — новый закон
владельца; 40 дворов не вводим).
