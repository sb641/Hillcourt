# STATUS — Хозяин HILLCOURT, 2026-09-22

## Назначение

Срез закона и хозяйства на приёмку смены 7 (ADR 0036): что вшито, что висит долгом, что врёт, куда следующий шаг.

## Что в законе

- Инварианты И-1…И-10 (`docs/00`): материя только переводом/рецептом/`external_in` по правилу; игрок меняет `Right`/`Obligation`/`Pack`/`Manor`, не `Person`; знание — только `Report` с датой; месяц — тик, дня нет; `sim/` без графики; детерминизм; объект материи с местом; лор только из онтологии; документ без проверки — брак.
- Столпы хозяина: право нарезается, двор — агент; вещи не появляются; Manor — книга, тэн — вложенный манор depth ≤ 1, земля содержит службу; игрок видит известия, не клетку; путь/рот/руки/весть/воз платны; дети — руки через 30 мес; yield не поднимать; Godot/бой/реликвии — только открытым этапом.
- Онтология (`docs/03`): ядро 8+2 + каталоги + `PlayerAction` по домам (`grant_tenure`, `send_party`, `send_march`/`send_sally`/`send_river`, `work_*`, книга манора). `Pack.route` — `find_path`, речной без origin.
- Тик (`docs/04`): 18 фаз — `season → growth → manor → hay → roadworks → labor → spoil → consume → demography → obligations → exchange → hazard → migrate → caravan → travel → decide → inform → record`.
- Канон смены 7 ЗЕЛЁНЫЙ (проверено хозяином сейчас): `hill` 36/1729 — живых 12, соль 15.6, голод 32, дельта 0; `two` 60/42 — живых 15, соль 15.6, голод 312, дельта 0; `shire` 60/1729 — живых 25, соль 15.6, голод 541, дельта 0. Окна §1 держатся (two 14–18, shire 24–28).

## Сделано агентами, но не вшито (долги — не окна)

1. Проводка `smelt_iron_bloom` к действию — рецепт есть, подключения нет.
2. TBD из ADR 0035 — финализировать измеренными числами смены 7 (окна выше — база).
3. Переименовать `test_offices_produce_reports` (имя врёт, поведения не меняет).
4. Stale-комменты: `needs.yml:41`, `v0_shire.yml:167-170`, `river.py:252` (мёртвая ссылка).
5. Потоки RNG на стойло; межклеточный торг (только своя клетка); выпас/приоритет косьбы — двойной вызов (`livestock_month` + `phase_hay`, удаление предложено).
6. Спавн `Hazard`/руин по `SpawnRule` не исполняется; течение/флот, боевой конь, камень/`quarry`, дневной контур — отложены.
7. `form` клетки — только дамп runner, не `Report`; `eye_from_hill` — только радиус 1.
8. Узкое окно two 17–19 — за автором демографии новым ADR (авторитет окон — §1).

## Что противоречит (на сверку Systems)

- `docs/09` §5 («рецепта железа нет») против §9/§10 (`smelt_iron_bloom`/`smith_iron_share` официальны) — stale-фраза.
- Окна: §1 (14–18) против «узкого 17–19» смены 6/7 — авторитет объявлен за §1, но узкое висит открытым.
- Хеши: `HASHES.txt` (история смены 2) против хешей смен 3–7 в ACCEPTANCE — снимок ревизии, не окно; новички читают как окно.
- `CHANGELOG` фиксирует «yield не поднят», `docs/09` фиксирует бонусы 1.0/1.05/1.15 — проверить числами, не словами.

## Проверка

```bash
bash sim/run_tests.sh
PYTHONPATH=sim/src python3 -m hillcourt.runner --scenario design/scenarios/v0_hill_and_salt.yml --months 36 --seed 1729
PYTHONPATH=sim/src python3 -m hillcourt.runner --scenario design/scenarios/v0_two_settlements.yml --months 60 --seed 42
PYTHONPATH=sim/src python3 -m hillcourt.runner --scenario design/scenarios/v0_shire.yml --months 60 --seed 1729
```

Критерий: сюит зелёный; соль 15.6 и дельта 0 везде; живые/голод — hill 12/32, two 15/312 (окно 14–18), shire 25/541 (окно 24–28).

## Следующий этап

Закрыть проводку крицы и долги имён без подъёма yield, затем — только служба тэна и цена пути.

## Цикл 1 (2026-09-22): вшил / отложил / выкинул

- Вшил: Systems №1–2, №10–16 (yield-оговорка, smith, `find_path`, река, кап 5, стройка, покупка, хеш-характер) — уже в законе; Table — канон ПРИНЯТО, И-3 не пробит.
- Отложил (долги с хозяином): smelt-проводка, TBD ADR 0035, имя теста должностей, двойная косьба, stale-комменты, узкое окно two 17–19.
- Поручил точечно: Info — в `messenger`-отчёт об уходе дописать `household id` + клетку (без новых каналов/задержек); Scribe/Economist — одной строкой снять stale `docs/09:93`; цифры ADR 0033 — stale, авторитет `needs.yml` + `docs/04` (старые ADR не правятся).
- Выкинул: новый слой механики — отказ по бюджету; молчаливая перебазировка окон — отказ.

## Цикл 2 (2026-09-23): куда развиваем, где пробелы

- Оценка хозяина: главный пробел — железо (крица без проводки морит комплекты тэна и лемех); второй — немая стройка пути (возврат не измерен); третий — молчащие спавны hazard/руин (нужен путь в закон, не код).
- В работе: Info — id двора в отчёте об уходе; Economist — проводка smelt_iron_bloom (гейт сытости, yield цел).
- Разведка без правок: World — аудит спавнов (путь в закон по бюджету); Systems — замер возврата стройки; Legal — стыки службы тэна.
- Table (плейлист) ждёт зелёного дерева после Info+Economist, затем играет.
- Следующий этап прежний: закрыть проводку крицы и долги имён без подъёма yield, затем — только служба тэна и цена пути.

## Цикл 2 — решения по разведкам (2026-09-23)

- World (спавны): `wolves_den` — рычаг, в очередь (топ-ап волков с капом + `Report`, владельцы Economist+Implementer, после smelt); `band_camp` — отложил до замера окон (кража 2.0 двигает голод); `sheriffs_ride` — выкинул (новый продюсер `Pack` = слой); `ruin_site` — выкинул (новая сущность); `travel_delay_months` — решение за Economist (рычаг или чистка); метка `scenario.py:271` — точечная починка в очередь Implementer.
- Legal (служба тэна 7/10 вшито): вшил подтверждение книги/нормы/`mustered`/`revoke`/`revoke_due`/помочей; противоречие `grant_tool` (код = ADR 0021 шире docs 07/08) — долг Scribe; stale 08:63 («рецепта нет») — снять когда smelt встанет; мелочь `depth`-гейт + docstring `update_musters` — в очередь Implementer.
- Scribe-заход (одной пачкой, когда код встанет): `docs/09:93`, `08:63`, `07/08 grant_tool` по ADR 0021. Тест должностей переименовать — туда же.
- В работе не тронуто: Info, Economist (smelt), Systems (возврат стройки).

## Цикл 3 (2026-09-23): поправка роли + допуски

- Поправка хозяина: владелец код не пишет и прогоны не гоняет — считают и запускают агенты.
  Приёмку smelt и работу Info проверяют Critic и Table, не владелец.
- Economist сдал долг №1 (проводка smelt, ADR 0037, заявлено 420 OK, канон бит-в-бит) — принят
  к приёмке, не вшит: ждут Critic и Table.
- Info-поручение (id двора в отчёте) пропало из списка агентов; работа в дереве, судьбу и
  поведение устанавливают Critic и Table.
- ADR 0038: допуски производительности открыты (точечно, через ADR с окнами, где врут руки);
  И-1/И-2/И-3 абсолютны; видение баронства — этапы, не закон v0.
- В работе у команды: Critic — ревью smelt + messenger; Table — приёмка (сюит, 3 канона, игра).

## Цикл 3 — вердикты Critic (2026-09-23)

- Economist (smelt, ADR 0037): ПРИНЯТО С ЗАМЕЧАНИЯМИ — И-1 чисто, дюпа нет, yield цел,
  канон бит-в-бит; ссылка `docs/09 §9` → 0037 в Scribe-заход; полный сюит — за Table.
- Info (id двора в отчёте): ПРИНЯТО С ЗАМЕЧАНИЯМИ — И-3 не пробит (вести, не `Tile.state`);
  `rejected_tile_ambiguity` — решить: `facts['tile']` = клетка рассказа (about), докнуть,
  дописать пины `lost`/`rejected`-веток; `docs/05:17–18` stale — в Scribe-заход;
  `hazards/` закрепить за владельцем в `AGENTS.md`.
- Вшивание обеих сдач — только после полного зелёного сюита от Table.

## Цикл 3 — приёмка Table, вшил (2026-09-23)

- Table: сюит **420 OK**, три канона в окнах смены 7, соль только Pack-цепочкой,
  smelt — 30 проводок в шире, id двора + клетка в отчёте — ПРИНЯТО по обеим сдачам.
- Вшил: проводку smelt (ADR 0037) и id двора в `messenger` (запись в CHANGELOG);
  `hazards/` закреплены за Implementer (`AGENTS.md`).
- Очередь: Info — `rejected_tile_ambiguity` (tile = about, докнуть, пины веток);
  Scribe-пачка — после Info; Economist — допуски (цена стройки первым ADR).

## Цикл 4 (2026-09-23): tile-about закрыт, Scribe вызван

- Info сдал: семантика `tile` = about + 2 пина (`rejected`, частичная гибель); сюит
  **422 OK**, канон бит-в-бит, И-3 цел. Вшил (запись здесь; CHANGELOG — следующим кругом).
- Scribe вызван на пачку (`09 §9` → 0037, `08:63`, `grant_tool` по 0021, `05:17–18`).
  Имя теста должностей — не Scribe, в очередь Implementer (с фазой `wolves_den`).
- В работе: Economist (допуск цены стройки), Scribe (пачка).

## Цикл 5 (2026-09-23): допуск цены стройки на ревью

- Economist сдал (ADR 0039): дорога 20дн+3, брод 14+2, мост 28+4, кап 20; заявлено 422 OK,
  канон бит-в-бит, торг и yield еды целы. Нарушение буквы поручения (тронут engine/ при
  запрете) — раскрыто в ADR, судит Critic.
- Critic вызван на ревью 0039 (включая вопрос полномочия). Table — после Critic.
- Scribe-пачка дополнится числами 0039 после сдачи Scribe (топ-ап, не прерывая заход).

## Цикл 5 — вердикт Critic по 0039 (2026-09-23)

- ПРИНЯТО С ЗАМЕЧАНИЯМИ: допуск точечен (цене), окна честны (каноны бит-в-бит с хешами),
  И-1/И-2/И-3 целы, тесты причинны (18/18).
- Полномочие: нарушение буквы §0.2 при косвенном приказе и полном раскрытии — именное
  исключение без прецедентной силы (только константы+docstring `roadworks.py`); впредь —
  только явная файловая строка хозяина. Карточка Economist дополнена запретом `engine/`.
- Долги: stale цен в `docs/04:29–30`, `docs/02:28–29` — Scribe-топ-ап; полный сюит — Table.
- Table вызван на приёмку 0039. Вшивание — по его зелени.

## Цикл 6 (2026-09-23): Scribe-пачка вшита, топ-ап ordered

- Scribe сдал: `09 §9` → 0037, `08` честные границы, `grant_tool` по 0021 (07+08),
  `05` событийный отчёт + семантика tile=about; сюит **422 OK**. Вшил.
- Топ-ап Scribe: числа 0039 в `docs/04`/`docs/02` + `09_law_and_land:150`.
- Очередь Implementer (с фазой `wolves_den`): имя теста, `depth`-гейт, docstring, метка.
- Table гоняет приёмку 0039.

## Цикл 6 — допуск №1 вшит (2026-09-23)

- Table: **422 OK**, каноны бит-в-бит с хешами, приказ виден в логе, торговая граница
  цела — ПРИНЯТО. Вшил (ADR 0039, запись в CHANGELOG).
- Scribe-топ-ап чисел 0039 в полёте. Следующий допуск — выход полей (Economist, новым ADR).

## Цикл 2 — замер стройки (2026-09-23)

- Systems измерил: мощение всего маршрута соли (450 трудодней + 60 брёвен) покупает 0 — `eta` упирается в пол `max(1, ceil)`, каденция фиксирована, фураж считается по входам, риск по клеткам, память дней не хранит. Долг 0029/0036 подтверждён числами.
- Решение хозяина: стройка в v0 принимается только по приказам (дни в логе `send_march`); длину пути из обоснования стройки для торга выкинуть; вариант C — честная граница (ROI-зонд красен по дизайну, не баг); вариант B (фураж по честным дням) — в очередь Economist отдельным ADR с замером окон; вариант A (маршрут за 30 дней) — выкинул, длинной карты нет.
- Методика в закон замеров: один вариант — один свежий `load_scenario`.

## Цикл 7 (2026-09-23): топ-ап вшит, хвост за Scribe

- Scribe сдал топ-ап (`04`, `02`, `09_law_and_land:150`), сюит 422 OK. Вшил.
- Хвост: `09 §4` (кап ≤12) — возвращён Scribe одной строкой.
- В работе: Economist (допуск №2 — выход полей), Scribe (хвост §4).

## Цикл 7 — хвост закрыт, допуск №2 на ревью (2026-09-23)

- Scribe: `09 §4` → кап 20 по ADR 0039; второй прогон 424 OK. Первый прогон (FAILED, 422) —
  гонка с правками Economist в полёте, не вина доки; плавание счёта 422↔424 — под надзор Table.
  Вшил.
- Economist сдал допуск №2 (ADR 0040): сентябрь 1.30→1.60, демена целы; ловушка августа/зимы
  отклонена замером; сдвиг открыт (hill 12/15.6/31, two 14/15.6/341 кромка окна, shire 26/15.6/547).
- Critic вызван на ревью 0040. Table — после Critic. Вшивание — по зелени.

## Цикл 7 — вердикт Critic по 0040 (2026-09-23)

- ПРИНЯТО С ЗАМЕЧАНИЯМИ: одна цифра каталога, демена целы, канон воспроизведён до цифры,
  ловушка подтверждена направлением, зона своя (контраст с 0039).
- Нагруженное: two ровно на 14, запаса ноль — фуражному ADR предзарегистрировать критерий
  (13 = открытая перебазировка, не замер).
- Долги: `manor_accounts §3` + проза пика (`02`/`09`) — Scribe-очередь; пины ловушки —
  значения, не механизм (держится на стенде).
- Table вызван на приёмку 0040 (сюит + кромка 14 + игра). Вшивание — по зелени.

## Цикл 8 (2026-09-23): допуск №2 вшит, три задачи

## Цикл 9 (2026-09-23): допуск №3 на ревью

- Economist сдал (ADR 0041): фураж по честным дням; заявлено 427 OK, two 14 (гейт держится),
  hill бит-в-бит, дельта 0. Critic вызван. Table — после Critic. Вшивание — по зелени.

## Цикл 9 — вердикт Critic по 0041 (2026-09-23)

- ПРИНЯТО С ЗАМЕЧАНИЯМИ: гейт two ≥ 14 удержан без натяжек, точечно фуражу, И-1 цел,
  тесты причинны, зона строго своя. Тонкости просмотрены (монотонность, река, hill-хеш).
- Решение хозяина по замечанию Critic: четвёртому допуску — сначала закрыть очередь
  Scribe (топ-апы 0039/0040 + `manor_accounts`). Table вызван на приёмку 0041.

Scribe: проза пика вшита (`manor_accounts §3`, сюит 427 OK); хвост `02` про проводку —
возвращён словом хозяина.

- Table: **424 OK** стабильно, каноны (12/31, 14/341 кромка, 26/547), триггер 13 не сработал,
  рента 132.0, соль Pack-цепочкой — ПРИНЯТО. Вшил (ADR 0040, CHANGELOG).
- Раздано: Implementer — микро-хвосты (имя теста, `depth`-гейт, docstring, метка);
  Economist — допуск №3 (фураж по честным дням, критерий two ≥ 14 предзарегистрирован);
  Scribe — проза пика (`manor_accounts §3`, `02`/`09`).

## Цикл 10 — хвост `02` и микро-хвосты вшиты (2026-09-23)

- Scribe: `02:36–38, :74–77` → проводка по ADR 0037; сюит 427 OK. Вшил.
- Implementer: имя теста, явный `depth`-гейт, docstring явки, метка → `None`.
  Critic — ПРИНЯТО без замечаний (гейт своим прогоном, hill бит-в-бит). Вшил (CHANGELOG).
- Жду: Table (приёмка 0041). Допуск №4 — только после закрытия очереди Scribe.

## Цикл 11 (2026-09-23): допуск №3 вшит, очередь Scribe закрыта

- Table: **427 OK** стабильно, hill бит-в-бит, two 14 (триггер не сработал),
  обозник подтверждает дни и конечность реки — ПРИНЯТО. Вшил (ADR 0041, CHANGELOG).
- Очередь Scribe закрыта (топ-апы 0039/0040, проза, хвосты) — стопор на допуск №4 снят.
- Открываю допуск №4: `wolves_den` (топ-ап волков с капом + `Report`) — правило за Economist.

## Цикл 11 — допуск №4 на ревью (2026-09-23)

- Economist сдал (ADR 0042): контракт топ-апа (потолки 1.0/6.0, новых Hazard нет,
  весть — за Info при фазе); без фазы молчит (замер 12 мес); заявлено 429 OK, бит-в-бит.
- Critic вызван. Table — после Critic. Фаза — заходом Implementer после вшивания правила.

## Цикл 11 — вердикт Critic по 0042 (2026-09-23)

- ПРИНЯТО (замечание одно — очередь фазы): вести ниоткуда нет, потолки запинены,
  молчание структурно, зона своя. Table вызван на приёмку (сюит + каноны + волки прежние).

## Цикл 12 (2026-09-23): допуск №4 вшит, фаза ordered

- Table: **429 OK**, хеши бит-в-бит, two 14, угроз 2→2, вести только с кромки — ПРИНЯТО.
  Вшил правило (ADR 0042, CHANGELOG).
- Раздано: Implementer — фаза топ-апа в `phase_hazard` (потолки, `rng.hazard`, новых нет);
  Info — весть о новой угрозе по `report_required` (без вести фаза не едет).

## Цикл 12 — фаза сдана, на ревью (2026-09-23)

- Implementer сдал (ADR 0043): топ-ап в начале `phase_hazard`, метка для Info в `world.stats`,
  вестей не рождает; заявлено 436 OK, числа бит-в-бит, сдвиг хешей открыт (hill 3, shire 6,
  two — бросок не выпал). Info-весть упомянута зелёной рядом — жду его handoff отдельно.
- Critic вызван на ревью фазы. Table — после Critic.

## Цикл 12 — вердикт Critic по фазе (2026-09-23)

- ПРИНЯТО С ЗАМЕЧАНИЯМИ: фаза по контракту (фильтр, поток, капы из `params`, новых нет),
  сдвиг хешей честен (формула точна), two без броска — случайность раскрыта (p≈4.6%),
  детерминизм, И-1/И-2/И-3 целы, фаза вестей не рождает.
- Мелочь в очередь: `counter_name_hardcoded` — имени счётчика параметризовать следующим правилом.
- Вшивание связки (фаза + весть) — после handoff Info и Table-приёмки.

## Цикл 12 — весть сдана, обрыв вскрыт (2026-09-23)

- Info сдал: `news/threats.py` (канал `messenger`, `confidence` 0.5, численность шумит
  только `rng.news`, дедуп), 4 теста-гейта; заявлено 438 OK, каноны цикла 8.
- Обрыв: живой тик не вызывает `report_pending_topups` (`phase_inform:571`) — волки
  растут молча. Добивка за Implementer (одна строка + пин живого тика).
- Вшивание связки — после добивки + Table (фаза + весть вместе).

## Цикл 12 — добивка сдана, на ревью (2026-09-23)

- Implementer: вызов в `phase_inform` + 3 пина живого тика (ADR 0044); заявлено 441 OK;
  сдвиг шира 26→25/547→539 открыт с механизмом (шум `rng.news` → флипы обоза), окно держится.
- Critic вызван на ревью добивки. Table — после Critic, играет связку целиком.

## Цикл 12 — вердикт Critic по добивке (2026-09-23)

- ПРИНЯТО С ЗАМЕЧАНИЯМИ: сдвиг честен (сошёлся до цифры), обоз читает только вести
  (нового канала нет), пины причинны, зона своя. Наблюдение: шир 25 — выше нижней
  половины окна; следующий сдвиг вниз будет кромкой.
- Table вызван на приёмку связки (сюит + каноны + весть глазами держателя).

## Цикл 13 — sync связки вшит (2026-09-23)

- Table: **441 OK**, каноны (12/31, 14/341, 25/539), шир 6 вестей/6 ростов, two 0/0 —
  ПРИНЯТО. Вшил связку (ADR 0042/0043/0044, CHANGELOG). Волки живые: угроза без вести не висит.
- Раздано: Scribe — sync угрозы в закон (`05` форма, `02` спавны, `04` фаза);
  Economist — замер `band_camp` (без кода: влезет ли кража 2.0 в окна).

## Цикл 13 — замер ватаги: входит (2026-09-23)

- Economist: гейт (`marsh` + ≥3 дворов) не выполняется нигде — правило не стреляет;
  принудительная ватага — влияние 0 во всех трёх мирах (топы пусты, соль мимо угроз).
  Вывод: входит замером, пересмотра окон не требует.
- Решение хозяина: pipeline ватаги как у волков — шаг 1 правило (Economist, потолки +
  `report_required`), затем фаза (Implementer) + весть (Info).

## Цикл 13 — sync связки вшит, правило ватаги на ревью (2026-09-23)

- Scribe: `05` форма вести, `02` спавны, `04` фаза; сюит 441 OK. Вшил.
- Оговорка: `02` пишет `band_camp` отложен — верно до вшивания фазы ватаги; правило
  (шаг 1, молчит) закону не противоречит. Sync фазы ватаги — после её приёмки.
- Economist сдал шаг 1 ватаги (ADR 0045): `mode: spawn`, гейт не стреляет (запинено),
  потолки, весть обязательна; заявлено 444 OK, канон стоит.
- Critic: ПРИНЯТО — `spawn` покрыт контрактно, форс-ватага проверена. Наказы фазе:
  рождение только с вестью (структурно), гейт per-tick.
- Table вызван на приёмку шага 1. Фаза — после вшивания, с наказами Critic.

## Цикл 14 — шаг 1 вшит, фаза и весть сданы (2026-09-23)

- Table: **444 OK**, ватаг 0, вестей 0 — ПРИНЯТО. Вшил правило (ADR 0045, CHANGELOG).
- Info: `make_birth_report` + `report_pending_births`, 4 теста; заявлено 454 OK, хеши бит-в-бит.
- Implementer: фаза (ADR 0046) — оба наказа кодом (рождение только с вестью — откат,
  гейт per-tick); заявлено 455 OK, канон бит-в-бит включая хеши.
- Critic вызван на ревью связки (структурность — главный вопрос). Table — после Critic.

## Цикл 14 — вердикт Critic по связке (2026-09-23)

- ПРИНЯТО: откат реален (без читателя — 0 сущностей), per-tick гейт, одна ватага,
  кража по своей клетке, связка живая (1 ватага → 1 весть). Дыра на будущее:
  `second_birth_id_reuse` — Table проверяет уникальность id.
- Table вызван на приёмку связки (сюит + каноны + форс + id).

## Цикл 15 (2026-09-23): связка ватаги вшита, дальше решения

- Table: **455 OK**, каноны стоят, форс-связка 7/7, id без живых дублей — ПРИНЯТО.
  Вшил связку (ADR 0045/0046, CHANGELOG).
- Раздано: Scribe — sync ватаги (`02`, `04`, `05` форма рождения); Economist — решение
  `travel_delay_months` (рычаг к `eta` или чистка, замер + окна, отдельным ADR).

## Цикл 15 — sync вшит, чистка на ревью (2026-09-23)

- Scribe: sync связки ватаги (`02`, `04`, `05`); сюит 455 OK. Вшил.
- Economist: чистка `travel_delay_months` (ADR 0047) — рычаг отклонён расчётом
  (+1 день никого не сдвигает); заявлено 455 OK, бит-в-бит. Critic вызван (микро-ревью).

## Цикл 15 — вердикт Critic по чистке (2026-09-23)

- ПРИНЯТО С ЗАМЕЧАНИЯМИ: удаление чисто, отказ посчитан; мелочь tombstone 0046→0047 —
  чинит Economist строкой. Table вызван на закрывающую приёмку (сюит + каноны).

## Цикл 15 — мелочь закрыта (2026-09-23)

## Цикл 16 (2026-09-23): чистка вшита, узкое окно — автору

- Table: **455 OK**, каноны стоят — ПРИНЯТО. Вшил чистку (ADR 0047, CHANGELOG).
- Economist вызван на старейший долг: узкое окно two 17–19 (автор демографии —
  доказать окна, урезать роды или закрыть узкое решением; авторитет §1).

## Цикл 16 — узкое окно на ревью (2026-09-23)

- Economist сдал (ADR 0048): узкое снять, §1 — авторитет; A/B (14/341/15 vs 17/288/0);
  подъём — гонка, урезка — убийство ядра; кода нет. Critic вызван (честность A/B).

## Цикл 16 — узкое закрыто, TBD закрыт (2026-09-23)

- Critic: ПРИНЯТО — A/B воспроизведён (OFF до цифры), гонка — измеренная регулярность,
  перебазировки нет. Вшил (ADR 0048, CHANGELOG).
- ADR 0049 (владелец): TBD 0035 закрыт измеренными числами цикла 15
  (455 OK; 12/31, 14/341, 25/539; окна §1 держат).
- Остаток долгов: двойная косьба, RNG на стойло, межклеточный торг, `counter_name_hardcoded`.

## Цикл 17 — мелочь закрыта (2026-09-23)

- Economist вызван: удалить второй вызов из `livestock_month` или доказать замером,
  что двойного съёма рук нет. Критерий предзарегистрирован (two ≥ 14, анти-голод пар).

## Цикл 17 — косьба срезана, на ревью (2026-09-23)

- Economist сдал (ADR 0050): удаление (добор косил 0, `_mow_reserve` жала поле зря);
  заявлено 455 OK, hill/two бит-в-бит, shire 539→545 в окне. Sync (`04`/`09`, docstring) —
  чужие зоны, долг записан. Critic вызван.

## Цикл 17 — вердикт Critic по косьбе (2026-09-23)

- ПРИНЯТО С ЗАМЕЧАНИЯМИ: удаление следует из замера (механизм + следствия воспроизведены),
  сдвиг в окне, зона своя. Мелочь: ссылка 0049→0050 — чинит Economist строкой.
- Table вызван на приёмку (сюит + hill/two бит-в-бит + скотник). Вшивание — по зелени.

## Цикл 17 — мелочь закрыта (2026-09-23)

- Economist: ссылки 0049→0050 в трёх строках, 14 тестов OK. Жду Table для вшивания косьбы.

## Цикл 18 (2026-09-23): косьба вшита, дальше потоки и sync

- Table: **455 OK**, hill/two бит-в-бит, пары `starved` = 0, сено — 378 проводок ранней
  фазой — ПРИНЯТО. Вшил косьбу (ADR 0050, CHANGELOG).
- Раздано: Scribe — sync одной косьбы (`04`, `09`); Economist — потоки RNG на стойло
  (свой поток приплоду/падежу, ADR с замером, two ≥ 14).

## Цикл 18 — sync косьбы вшит (2026-09-23)

- Scribe: единственная косьба в `04`/`09`, резерв удалён словами; сюит 455 OK. Вшил.
- Жду: Economist (потоки RNG на стойло).

## Цикл 18 — жребий сдан с красной меткой (2026-09-23)

- Economist (ADR 0051): стойло изолировано (хеш-поток, детерминизм, дельта 0).
  Красное: 456/457 — seed-пин трио (1729) голодает; критерий 12 мес зелёный;
  shire на кромке 24/489. Вероятности не крутил. Пути: (a) перекалибровка запасов,
  (b) смягчение минимума (exemption), (c) принять красноту.
- Critic вызван судить: механика или удача, какой путь честен. Вшивания нет — сюит красен.

## Цикл 18 — суд вынес путь (2026-09-23)

- Critic: красное системно (6/15 сидов, конный двор; не жребий стада — те же 6
  с выключенным приплодом); механизм — тонкий запас сена + min-1-голова, вскрыт
  сдвигом потока; запасы подогнаны под старый поток. Путь: (d) пин на механизм +
  (a) доливка с запасом; (b)/(c) — отказ; вшив только зелёным пакетом.
- Economist исполняет (d)+(a). Table — после зелени.

## Цикл 19 — выпас и пакет (2026-09-23)

- Правка хозяина: модель кормила скот сеном круглый год — неправда; сено — низкий сезон,
  летом подножный корм с коэффициентом. Сезонная модель выпаса — следующим заходом
  Economist (новым ADR, с замером и окнами); маржи пересчитать под ней.
- Economist (ADR 0052): пин делён, доливка с запасом и пином конечности; заявлено
  458 OK, стенд 3/15, two 14, shire 24.
- Critic: ПРИНЯТО С ЗАМЕЧАНИЯМИ — путь исполнен структурно, но счёт стенда не сошёлся
  (3/15 против 5–6/15, состав не раскрыт): исправить таблицу в ADR 0052; конфликт пинов —
  решение хозяина строкой. Economist чинит таблицу. Table — после.

## Цикл 19 — таблица исправлена, Table закрывает (2026-09-23)

- Economist: 15 сидов поименно + расширение до 25 (6/25 красных 2,5,13,14,16,22);
  счёт Critic подтверждён порядком, 3/15 признаны удачной выборкой. Кода не тронуто.
- Table вызван на приёмку пакета (сюит + каноны + скотник). Вшивание — по зелени.

## Цикл 20 (2026-09-23): пакет вшит, выпас ordered

- Table: **458 OK**, каноны (12/31, 14/341, 25/489 кромка), пары держатся маржой,
  таблица честна — ПРИНЯТО. Вшил пакет (ADR 0051/0052, CHANGELOG).
- Открываю правку хозяина (цикл 19): сезонная модель выпаса — Economist, новым ADR
  (коэффициент сезона на корм, пастбища как источник, маржи пересчитать).

## Цикл 20 — выпас шаг 1 на ревью (2026-09-23)

- Economist считает сезонный выпас (цикл 20).
- World/картограф готовит землю баронства: вид и данные под пастбища, стоянки, шахты,
  пристани, дороги (тропа vs строеная), деревни→города, замок + скетч сценария;
  механика (тропы от ходьбы, рост, добыча) — только предложкой хозяину, кода нет.

## Цикл 20 — выпас шаг 1 на ревью (2026-09-23)

- Economist (ADR 0053): сено — зимняя норма, летом коэффициент 0.3; нужда −29%;
  заявлено 476 OK, hill/two бит-в-бит, shire 24/489; `animals_starved` 10→0 стендом;
  корм дворов — следующим шагом Implementer, заявлено открыто.
- Critic вызван (источник пастбищ в данных? частичность честна?). Table — после Critic.

## Цикл 20 — земля сдана, коллизия чинится (2026-09-23)

- World сдал: FORMS 17→26, `LAND_MARKS` без полей на `Tile`, стадии роста (городов нет —
  `large_village`), ёмкость выпаса, `mine_sites`; заявлено 476 OK, каноны бит-в-бит;
  механики — только предложкой. Стык с Economist чтением (`pasture_forage_capacity`).
- Коллизия ADR 0053 (выпас раньше): картограф переименовывает свой в 0054.
- Дальше: Critic — ревью земли (зона `engine/tile_view.py`, вид без бонусов) + ревью
  выпаса уже в работе; Table — по обоим.

## Цикл 21 (2026-09-23): бесплатной траве отказ, коллизия закрыта

- Critic по выпасу: ПРИНЯТО С ЗАМЕЧАНИЯМИ — трава без источника (срез нужды, не ёмкость);
  частичность честна; стенд автора directional (10→4 у него против 10→0).
- Решение хозяина: шаг 1 отдельно не вшивается — только пакетом с шагом 2 (источник
  ёмкости + дворовый корм; именное разрешение на `_feed_livestock`, без прецедента).
  Economist исполняет шаг 2.
- Коллизия закрыта: земля — 0054.
- Critic по земле: ПРИНЯТО (вид без бонусов, тик не задет, И-5/И-8 целы).
  Роль World формализована в `AGENTS.md` (только вид/данные `tile_view.py`).
  Table вызван на приёмку земли.

## Цикл 21 — земля вшита (2026-09-23)

- Table: **476 OK**, каноны стоят, путник видит новые формы (бонусов нет),
  детерминизм повтором — ПРИНЯТО. Вшил землю (ADR 0054, CHANGELOG).
- Жду: Economist (выпас шаг 2 — источник + дворовый корм, пакетом с шагом 1).

## Цикл 22 (2026-09-23): выпас с краем на ревью

- Economist (ADR 0055): лето 30% сено + 70% выпас с капом 2.0, недобор → сено;
  дворы — по именному разрешению; заявлено 478 OK, стенд 4–9, каноны стоят.
- Critic вызван (край кодом? разрешение не превышено? трава не бесплатна?).

## Цикл 22 — вердикт Critic по выпасу (2026-09-23)

- ПРИНЯТО С ЗАМЕЧАНИЯМИ: край кодом (кап, порядок, перевод `graze`), трава не бесплатна,
  разрешение не превышено, И-1/И-6 целы, каноны стоят.
- Обязательное: единицы стенда в ADR 0055 (за 60 мес, не за год) — микро-ADR errata
  новым файлом (старые не правятся). Economist пишет errata. Table — после.

## Цикл 22 — errata готова, Table закрывает (2026-09-23)

- Economist: ADR 0056 — единицы за 60 мес; кода не тронуто.
- Table вызван на приёмку пакета выпаса (сюит + каноны + скотник). Вшивание — по зелени.

## Цикл 23 (2026-09-23): выпас вшит, sync — Scribe

- Table: **478 OK**, каноны стоят, `graze` строго 5–9, `starved` 7.0, `grazed` 75.3 —
  середина — ПРИНЯТО. Вшил пакет (ADR 0053/0055/0056, CHANGELOG).
- Scribe вызван: sync выпаса (`09` корм/выпас, `04` фазы, `02` сено зимой).
- Остаток: межклеточный торг (отложен, нужен ADR), `counter_name_hardcoded` (следующим правилом).

## Цикл 23 — sync выпаса вшит (2026-09-23)

- Scribe: сезонный корм в `09`/`04`/`02`, строка §10, бесплатной травы нет; сюит 478 OK.
  Вшил. Закон догнал выпас полностью.

## Цикл 24 — сверки сданы, Scribe пакует (2026-09-23)

- Economist: проект соседского торга принят (кластер, носитель — двор, окна держат);
  вшивает с ценой носителя (против +34 голода).
- Implementer: docstring `phase_hay` — сдан (25 тестов, поведения ноль). Вшил.
- Legal: цепочка недоимки исполняется, в 07 — только правый край (долги №1,2,10 + мелочь №9);
  противоречий X vs ¬X нет.
- Systems: блоки «Проверка» 11/11; Д1 — ложная формулировка; Д2/Д3 — `barter_memory` и
  `remembered_road_tiles` отсутствуют в `03`.
- Scribe вызван пачкой: Д1 строкой + микро-ADR служебной памяти + вшивка в `03` +
  раздел недоимки в `07` по таблице Legal.

## Цикл 25 (2026-09-23): Scribe-пачка вшита, красное — торг в полёте

- Scribe: Д1, микро-ADR 0058 (номер 0057 занят торгом), `03` память, `07` недоимка.
  Доки вшил (тесты `docs/` не читают — проверено им).
- Красное `test_hh12_neutral_by_mechanism` — не Scribe: в полёте правится `exchange.py`.
  Разбор — по handoff Economist; вшивания торга нет до зелени.

## Цикл 25 — торг сдан, на ревью (2026-09-23)

- Economist (ADR 0057): кластеры, цена носителя (сбор/укус/кап 2.0), только `transfer`;
  пин нейтральности заменён механизмом цены; заявлено 479 OK, shire +41 на кромке 24.
- Critic вызван (цена кодом? замена пина — механизм? сдвиг честен?).

## Цикл 25 — вердикт Critic по торгу (2026-09-23)

- ПРИНЯТО С ЗАМЕЧАНИЯМИ: цена кодом, замена пина — механизм, сдвиг честен, телепорта нет.
- Обязательное: `gift_roll_contradiction` (ADR «бросков нет» vs бросок дара 0.3) —
  закрыть текстом (ветка предшествовала) или кодом + пин. Economist чинит. Table — после.

## Цикл 25 — противоречие закрыто, Table закрывает (2026-09-23)

- Economist: бросок предшествует 0057, в ADR — «новых точек бросков нет»; кода не тронуто.
- Table вызван на приёмку торга (сюит + каноны + торговец). Вшивание — по зелени.

## Цикл 26 (2026-09-23): торг вшит, sync — Scribe

- Table: **479 OK**, каноны (12/29, 14/341, 24/530 кромка), голодный сбора не платит,
  соль только обозом — ПРИНЯТО. Вшил торг (ADR 0057, CHANGELOG).
- Scribe вызван: sync торга (`09` обмен — кластер/цена/кап, `02` — соседский торг разрешён).
- Остаток: `counter_name_hardcoded` (следующим правилом).

## Цикл 26 — sync торга вшит (2026-09-23)

- Scribe: кластер/цена/кап в `09`, граница снята в `02`; сюит 479 OK; вчерашний
  красный позеленел заменой пина (как заявлено). Вшил. Закон догнал торг.

## Цикл 27 (2026-09-23): этап троп открыт

- Решение хозяина: тропы от ходьбы входят — профили (пеший, солдаты в броне, возы,
  всадники-гонцы/военные), уровни (тропа → грунтовка → строеная), стройка приказом.
- World сдал проект + замер (скидка не ломает торг: маршруты < 30 дн. иммунны;
  каноны бит-в-бит). Решение: входит.
- Раздано: Implementer — механика (уровни, веса, профили-строки, множители, память возниц);
  Info — весть о новой тропе (глаз — весть, даль — молчание, `known_tiles` не расширять).

## Цикл 27 — весть сдана, жду механику (2026-09-23)

- Info: `news/trails.py` (канал `eye_from_hill`, `tile` = about, даль — молчание,
  `known_tiles` не расширяется), 5 тестов; заявлено 484 OK, каноны стоят.
  Ждёт меток `trail_new_<tile>` + строку вызова (чужая зона — не правит).
- Вшивание связки — после механики Implementer + Critic + Table.

## Цикл 28 (2026-09-24): тропы в коде, следующий этап — нативные деревни

- Implementer в полёте: тропы живые (17 клеток, часть выцветает до тропы — физика),
  пишет тесты; дельта 0.
- Параллель (только проекты, кода нет): World — нативная деревня без новых сущностей
  (чем выражается из существующей онтологии + замер стоимости); Systems — стади-гейт
  видения барона (что в законе, что предложено, что бюджетом отказано); Legal — обычное
  право нативной деревни из существующих пресетов.
- Проверка хозяина: модель Implementer сменилась на `space-bunny-free` — мандат
  «только Muse Spark 1.3 Contribute Free» нарушен; жду решения хозяина (вернуть или оставить).

## Цикл 29 (2026-09-24): нативная деревня v1 — решение владельца

- Legal: простая свободная община влезает без новой сущности (`free_landless` +
  `Settlement.kind=village` + communal-доступ); коллективный титул не влезает
  (`Right.kind=common` декларативен, группа-носитель нужен через ADR); присяга без
  последствий — ложь, не берём.
- Решение владельца: **v1 без новой сущности** — племя = деревня на `village`, дворы
  `free_landless`, доступ через communal-доступ/`grazing`-право, рычаг игрока —
  выдача/снятие доступа и `levy` (существующий `Obligation`), не барщина и не связывание.
  `Community`/присяга/коллективный титул — позже и только через ADR, если v1 покажет нужду.
- В работу после отчётов World и Systems: ADR (Scribe) → данные сценария + форма вида
  (Implementer/World) → Critic/Table.

## Цикл 30 (2026-09-24): стади-гейт и племя как факт

- Systems-стади-гейт: видение баронства **закрыто по бюджету** — города 100–200, уровни
  домов, таверна-механика, служба 60–90 дней, ~10 баронств требуют своих ADR. Замок дёшев
  (основа в законе), мастеровые — расширение каталога. Две дыры ввода: `LAND_MARKS` не
  грузятся сценарием; тропы в коде, но не в законе.
- World: племя маркером — ярлык без закона; `Right.common` объявлен, но не исполняется
  (`own_tiles` считает его индивидуальным держанием) — это ложь онтологии.
- ADR 0060 (владелец): `Settlement.kind=native_village` (данные, не скрытый маркер) +
  `common` становится исполняемым общим доступом, а не наделом; форма — из вида поселения.
- Раздано: Economist — `common` в `own_tiles`/`_held_tile_ids` + тест; Info — тексты по
  виду поселения (не маркеру); Implementer — загрузчик/сценарий после сдачи троп.
- Тропы: Implementer в коде (тесты пишет), Info-вести сданы (`news/trails.py`, 484 OK);
  дальше Critic → Table → ADR + sync `02`/`03`/`04`.

## Цикл 31 (2026-09-24): тропы сданы, коллизия номера

- Implementer сдал тропы: `Tile.trail_wear` + пороги 3/12 + затухание 0.5 + веса (воз 4.0,
  отряд 2.0, ходок 1.0, река 0) + профили-строки `arms`/`rider` + множители 0.9/0.8 +
  топтание в тике + память грунта; заявлено 496 OK, каноны без сдвига чисел.
- **Коллизия ADR:** Implementer занял 0059, который уже мой (нативная деревня) —
  его переименование в 0061, следующий его шаг — загрузчик/сценарий племени.
- Economist сдал `common` (ADR 0060): 498 OK, каноны держат, хеши сдвинуты открыто.
- Critic вызван на два ревью в одном дереве (A тропы, B право `common`).
- World вызван: вид троп из `trail_wear` (не скрытый маркер) + форма племени из
  `Settlement.kind`. Info пишет тексты племени.

## Цикл 32 (2026-09-24): племя как независимый агент (ADR 0062)

- Решение владельца по идее хозяина: `Tribe` с четырьмя стойками —
  `independent` (по умолчанию: живут, торгуют, не платят, не дают солдат),
  `allied` (своя книга/экономика, земли, отвечают на вызовы, оброк),
  `vassal` (то же + признаёт старшего вассала; отказ = нарушение, подавление),
  `annexed` (распущено, дворы — обычная деревня). Племя не «просто уходит», как деревня.
- Запрещено: второй корневой Manor/depth>1, `tied`/барщина в v1, дипломатия без ADR.
- Разведки: Legal (запрет ухода, оброк, вызов, мятеж), Systems (бюджет-гейт полей,
  И-3 `loyalty`), Economist (числа оброка и вызова). Код не трогают.

## Цикл 33 (2026-09-24): вердикты Critic по тропам и праву

- Тропы (ADR 0061): **ПРИНЯТО** без дыр (вес на возе, дорога/вода вне системы,
  детерминизм повторён, клэмп со скидкой взаимоисключающи).
- `common` (ADR 0060): **ПРИНЯТО С ЗАМЕЧАНИЕМ** — `grazing` по коду индивидуальное
  держание, а не доступ; ADR 0062 уточнён: земли союзника = `common` + `works_tiles`,
  `grazing` — только личным решением барона; «grazing = доступ» запрещён.
- Table вызван на единую приёмку обеих сдач (сюит + каноны + путник + община).

## Цикл 34 (2026-09-24): племя v1 очерчено (ADR 0063)

- Legal: запрет ухода — lookup по составу племени (без пресета/`Household.tribe_id`);
  смешанный оброк = две `Obligation`; вызов — отдельное действие, `send_sally` нельзя;
  отказ/мятеж — v2 (дорого).
- Economist: в v1 оброк только натуральный 0.6 зерна/мес на двор (серебра нет — денежный
  оброк лгал бы); `muster_profile` = «сколько взрослых и комплектов» без новых товаров;
  `loyalty` не трогает голод/труд/окна.
- ADR 0063 (владелец): v1 = данные+запрет ухода+натуральный оброк+проверка «может дать
  вызов»; v2 (отдельный ADR) = дипломатия, вызов-действие, отказ/мятеж, деньги, `Community`.
- Жду: Systems-бюджет-гейт по `Tribe`; Table-приёмка троп+`common`.

## Цикл 35 (2026-09-24): `Tribe` урезан по бюджет-гейту (ADR 0064)

- Systems-гейт: тонкая надстройка совместима с законом, но набор полей 0062 рисковал
  дублями книг, скрытым tech-tree и всеведением.
- Решение владельца: `Tribe` v1 = 7 полей (`id, name, stance, settlement_id,
  tribute_grain, muster_kits` + имя), состав и общая земля вычисляются, `loyalty`/`muster_profile`/
  `own_economy`/`granted_land`/`annexed` — вне v1 (v2), биекция с `native_village` обязательна.
- Жду: Table-приёмка троп+`common` → вшивание; затем код племени v1 (Implementer: загрузка +
  guard ухода; Economist: оброк/проверка «может дать»).

## Цикл 36 (2026-09-24): тропы и община вшиты, племя на ревью

- Table: **505 OK**, каноны (12/29, 14/341, 24/530), тропы быстрее но не до нуля, вода/строеная
  вне системы, вдали молчание; `common` — община не первый двор. Обе сдачи ПРИНЯТО.
  Вшил (ADR 0061/0060, CHANGELOG).
- World сдал вид из данных: `trail`/`dirt_road` из `trail_wear`, `built_road` приоритетом,
  `tribal_village` из `Settlement.kind` (скрытый маркер мёртв); 505 OK. Critic вызван.
- Scribe (новый агент) синхронизирует `02`/`03`/`04`/`07` по 0061/0060/0059/0062/0064.
- Implementer в полёте: загрузка `native_village` + guard ухода (`test_native_village_scenario` 10/10).

## Цикл 37 (2026-09-24): мастеровые и дома — решения

- Economist: мастеровые **уже в законе как рецепты/действия** (кузнец, плотник, мельник,
  прядильщик, мясник, солевар) с честным гейтом; признак «мастер» в v1 не вводим
  (поле без механики = ярлык). Мастерские/гильдии/рынок мастеров — отложены.
- World: уровни домов и таверна — только вид. ADR 0065 (владелец): маркер `dwelling`
  (`tent_earth`/`house`/`multi_storey`) + `tavern_site`; авторитетный `housing_level` —
  отдельный ADR позже. World реализует формы; загрузчик `marks:` — за Implementer'ом.
- «Видно, что двор — кузнец» — знание по вести (Info), не поле экономики.

## Цикл 38 (2026-09-24): слой вызова/отказа оформлен (ADR 0066, владелец)

- Legal (проект v2): вызов = по одной `muster`-повинности на каждый племенный двор
  (состав через биекцию), новое действие `call_tribe_muster`, `send_sally` не трогаем;
  `Pack` только при фактическом выходе; отказ (`refused`) ≠ невозможность (`unable`)
  ≠ молчание (И-3).
- ADR 0066: `Obligation` += `kind=muster`, `required_persons_count`, `call_status`;
  Scribe правит онтологию; код v2 — только после приёмки v1 (порядок: v1 → Critic → Table
  → v2 микро-шагами: Legal → Implementer → Economist → Info).
- В полёте: Implementer (`Tribe` 7 полей + биекция + guard ухода), World (формы домов),
  Scribe (sync доков), Economist (проект завершён), Legal (проект завершён).

## Цикл 39 (2026-09-24): Scribe-синк вшит, стык меток найден

- Scribe синхронизировал `02`/`03`/`04`/`07` под 0061/0060/0059/0062/0064; сюит **515 OK**;
  DoD цел.
- **Найден реальный стык:** вести о тропах читали `trail_new_*`, движок пишет `trail_born_*`
  (ADR 0061, принят Critic'ом) — весть молчала в живом тике; плюс не было вызова в
  `phase_inform`. Движок — источник истины: новый Info-агент правит читателя и добавляет
  тест, ловящий расхождение имён; Implementer добавляет строку вызова (ADR 0044-прецедент).
- Scribe отметил: `Tribe` описан в `03`, кода пока нет (срез Implementer в полёте).

## Цикл 40 (2026-09-24): формы домов сданы, аудит и допуски в работе

- World: `tent_earth_homestead` / `timber_house` / `multi_storey_house` из маркера `dwelling`
  (только для одиночного двора; 2+ — `village`/`tribal_village`); `tavern_site` без изменений;
  строгий повтор — **532 OK**; попросил секцию `marks:` в загрузчике (очередь Implementer'а).
- Systems: аудит честности синка + проверка тупика племени (голод при запрете ухода).
- Economist: проект допуска производительности рук (20 трудодней/мес, партии, стройка) —
  числа и кандидаты, без кода.

## Цикл 41 (2026-09-24): допуск рук измерен, открыт один (ADR 0067)

- Economist-стенд: партия 20 дн → 2/мес (кап 4 мёртв), тракт 15 клеток = 15 мес при
  1696 свободных рук/мес — врал кап, не руки.
- Решение владельца: **открыт кап стройки 20→40** (ноль сдвига канонов, тракт 15→8 мес,
  эффект виден только по приказу). Взрослый 20→24 — не сейчас (глобальный множитель,
  голод растёт везде — допуск «сытый мир»). Партия 20→15 — отклонено (шир 23, вне окна).
- Очередь Implementer'у: константа капа + тест; затем секция `marks:` в загрузчике
  (просил World). Scribe синк чисел после приёмки.

## Цикл 42 (2026-09-24): аудит нашёл две лжи (ADR 0068, владелец)

- Info-починка вестей о тропах: читатель переведён на движковые метки `trail_born_*`/
  `dirt_born_*`, тест ведёт настоящее топтание через `phase_inform → PlayerView`;
  wiring уже в `tick.py`; **536 OK**.
- Systems-аудит: (1) `Right.common` объявлен доступом, но `has_access_to_communal_tile` его
  не читает — право нарезано, а не меняет ничего; (2) оброк племени и проверка «может дать»
  — обещание без кода; (3) формы домов без входа `marks:` (в очереди Implementer'а);
  (4) племенной двор может голодать бесконечно, а пустое поселение остаётся «живым» в виде.
- ADR 0068: `common` даёт доступ (Legal), пустое племя честно (без новой смерти); зона
  `legal/` закреплена за Legal. Раздано: Legal — доступ; Economist — оброк + «может дать».

## Цикл 43 (2026-09-24): доступ `common` сдан

- Legal: `has_access_to_communal_tile` учитывает `Right.common` (доступ по поселению/двору),
  `tenure` — доступ+держание, `grazing` без изменений; **548 OK**, каноны/дельта прошли.
  Critic вызван (нет ли ворот шире прежнего). World вызван — пустое поселение не «жилое».
  Economist в полёте: оброк + «может дать».

## Цикл 44 (2026-09-24): `common` принят, приёмка идёт

- Critic: ПРИНЯТО С ЗАМЕЧАНИЕМ — доступ да, держание нет; `tenure`/`grazing` согласованы;
  ворот нет (`common` не всеобщее, лорд чужой общины не проходит). Дыра на будущее:
  `settlement_scope_unbounded` — доступ поселения к его `common`-клеткам без радиуса;
  решение владельца при стадии крупных поселений.
- Table вызван на приёмку `common` + вести троп (после зелени текущих сдач).

## Цикл 45 (2026-09-24): оброк племени сдан

- Economist: `economy/tribe.py` — оброк 0.6 зерна/мес на племенный двор только при
  `allied|vassal`, в амбар корня, `arrears` при неоплате, пустой двор не платит, смена
  стойки снимает повинность; `tribe_muster_kits`/`tribe_can_muster` — только данные
  (взрослые + комплект), без вызова/`Pack`/`send_sally`. 9 зондов, **548 OK**, каноны
  без сдвига, детерминизм. Critic вызван.
- В полёте: World (пустое поселение — честный вид), Implementer (кап стройки 0067 +
  секция `marks:`), Economist завершён, Table ждёт зелени дерева.

## Цикл 46 (2026-09-24): кап стройки и `marks:` сданы

- Implementer: `MONTHLY_LABOR_CAP` 20 → 40 (ADR 0067; дни/материалы 0039 целы, пины),
  ROI-зонд печатает «cell ≤1mo»; секция `marks:` в `scenario.py` (ADR 0065) с гейтами и
  сценарием `v0_native_village.yml`; `TestMonthlyLaborCap` + `test_scenario_marks` (8).
  **559 OK**, каноны побит, дельта 0; прошлый единичный ERROR — гонка с Info, не воспроизводится.
- Critic вызван на обе сдачи. World дописывает пустое поселение. Далее Table на единую приёмку.

## Цикл 47 (2026-09-24): Table зациклился, стол пересоздан

- Critic по капу 40 и `marks:`: **ПРИНЯТО** обе (числа 0039 целы, клэмп по-проектный,
  маркеры — данные без чтения тиком, 4 гейта загрузчика).
- Старый Table дважды вернул устаревший отчёт (505) — цикл на старой сессии; агент отменён,
  создан чистый стол (Space Bunny Free, high) с требованием первой строкой дать реальное
  число тестов. World ещё дописывает «пустое поселение» — не судится этим прогоном.

## Цикл 48 (2026-09-24): масштаб баронства — калибровка вместо раздувания

- Решение владельца по вопросу хозяина: **тайл ≈ 1 км (сельский округ)**, баронство 12×12
  ≈ 150 км², диагональ 17 тайлов ≈ 1,5–2 суток пешком / 2–3 обозом. Нынешний шир (99 тайлов)
  — уже баронство; значит этап = **перекалибровка скоростей** (сейчас 1,2–1,8 суток/тайл),
  а не увеличение карты.
- Мир сверху: округ 6–8 баронств (30×30 км) — агрегированный слой, по агенту на баронство,
  без потайлового тика; въезд в чужое баронство — поездка.
- Знание: 0–1 тайл факт, 2 тайла слух ~50%, дальше тишина; каналы разной глубины (караван —
  цены/дороги/опасности, официальный доклад — полный статус, охотники — смутные sighting'и).
- Разведки: World (что значит тайл при 1 км, пастбища/кап числа), Systems (часы на тайл по
  профилям, что ломает ETA/фураж/тропы, кольца знания, туман войны). ADR «Масштаб» — после чисел.

## Цикл 48 — калибровка от World (тайл = округ)

- Пастбище 1 тайл (кап 2.0/мес) держит: 2 вола / 2 коровы / 2 лошади / 4 осла / 5 свиней /
  7 телят (предел общий). Деревня из 5 дворов: 1 тайл + пастбище + лес = 3 тайла (план карты,
  не правило).
- **Долг терминов при новом масштабе:** `tile_plot`/виргата (30 акров) врёт при тайле ≈247 акров;
  `holding_scale` — доля потолка округа, не площадь; `harvest_grain` — крупная работа на округ,
  не партия поля; `pasture_forage_capacity` — условные единицы, не биология. Правится после
  ADR «Масштаб» (Economist + Scribe).
- Жду Systems (часы на тайл по профилям, ETA/фураж/тропы, кольца знания, туман войны).

## Цикл 49 (2026-09-24): этап времени открыт (ADR 0069, владелец)

- Systems-калибровка: нынешний путь в **сутках** (1 день за поле, 8 за лес) — в 8–96 раз
  длиннее reality; месячный ETA стирает и 2 суток, и 60–90 суток; задержка вести — месячная.
- Решение владельца: тайл = 1 км, движение в `travel_hours`, `Pack.eta_date` по дням с
  дневным контуром in-transit (прямо разрешён И-4), `Report.delay_days`, кольца знания по
  радиусу источника (0–1 факт, 2 — слух ~50%, дальше тишина); терминологический долг
  (`tile_plot`/виргата) чинится в этом этапе.
- Порядок реализации после зелени текущей приёмки: Implementer (часы/ETA/дневной контур) →
  Economist (фураж, термины) → Info (delay_days, радиусы) → Scribe (доки) → Critic/Table.
- Не трогаем: тропные пороги (счёт проходов ≠ время), каденция обозов, округ баронств (отдельный ADR).

## Цикл 50 (2026-09-24): масштаб пересобран — гекс = место (ADR 0070, владелец)

- Правка хозяина: тайл = **гекс ≈ 0,4 км** (деревня 5 дворов; 50 дворов = 10 гексов; город =
  кварталы 20–40 семей с мастерскими; гекс колонны = отряд/обоз). Баронство 100×100 ≈
  40×40 км, переход 2–3 суток. ADR 0069 п.1 («тайл = 1 км») **отменён**.
- 0070: ёмкость гекса по контексту (деревня ≤5 дворов; квартал 20–40 семей; колонна — поход);
  большая карта = слои (поселения-агенты + coarse-ландшафт), не агент на гекс; квартальная
  специализация — существующими рецептами; гекс колонны — с v2 вызовом.
- Разведки: Systems (часы на гекс, слои, кольца знания), World (смысл гекса, пастбища,
  эскиз 10 000 гексов, кварталы). Реализация — после зелени текущей приёмки.

## Цикл 51 (2026-09-24): сетка и место — законы готовы (ADR 0071/0072)

- Systems: часы на гекс (нога 0,65 ч в поле, конь 0,40, обоз 0,85, армия ×1,25), 100 гексов
  = 2,1–3,3 суток, диагональ 141 гекс = 3–5 суток; слои 10 000 гексов (поселения-агенты +
  coarse-ландшафт); кольца знания по радиусу источника.
- World: сельский гекс ≤ 5 дворов (кап подходит), квартал 20–40 семей упирается в общий кап;
  `House` нет (двор ≠ дом) — не вводим, «дом» = 2–4 двора на гексе; мастерские через
  `Recipe.place=settlement` + `HouseholdAction.recipes`; пастбище 2 вола/4 осла/5 свиней на гекс.
- ADR 0071: шестиугольная сетка (6 соседей), только топология, без правил; часы, а не сутки.
- ADR 0072: правило места — деревня ≤5 дворов, квартал 20–40 семей, специализация рецептами.
- **Блокер приёмки:** студийный Table зациклен на старой сессии и дважды вернул отчёт
  505; новый Table не заводлю (запрет хозяина) — нужно перезапустить соседний чат стола.

## Цикл 52 (2026-09-24): баронство как земля (ADR 0073, владелец)

- Решение владельца по видению хозяина: карта 100×100 гексов — данные (не агенты); месяц —
  поселения/экономика/право; день — только движущееся (`Pack` в пути, колонна, разведочная
  партия, активная опасность — И-4 цел). Разведка = источник знания: видимость по террейну и
  профилю, 2 гекса — слух, дальше тишина; гекс известен только из вести. Редкие места
  (отшельник, застава) — через поиск рядом, находка = поселение в 1 двор. Река/порты/караваны
  в соседние баронства — в карте; выход за карту — отдельный ADR.
- Проекты: Systems (правила наблюдения, разведочная партия, находки), World (скелет карты
  100×100: река, порт, лес, пашни, wilds; перенос трёх канонов).

## Цикл 53 (2026-09-24): разведка законом (ADR 0074, владелец)

- Systems-проект: движение уже есть, не хватает смысла и наблюдения. Решение владельца:
  `Pack.purpose` + `Pack.profile_id` (без новых сущностей); на своём гексе — факт, в 1 гексе —
  вероятность по профилю/покрову, в 2 — слух (confidence≈0.5), дальше тишина; без `Pack`
  наблюдений нет; `Report.observer_id` = партия (не `"player"`); башня-дозор — отдельный ADR.
- Реализация после приёмки: Implementer (поля + правила), Info (канал и честный свидетель),
  Scribe (`03`/`05`). Находки (отшельник) — условия и цена ещё проектом.

## Цикл 54 (2026-09-24): водный вопрос хозяина — проект этапа

- Что есть: вода/брод/мост, `raft`/`boat` (рецепты из брёвен), профили `water_raft`/`water_boat`,
  `send_river` (судно ≥1.0, маршрут по воде), 17 зондов. Уровень — переправа.
- Чего нет: река как система (исток→притоки→устье→выход), порт как функция (склад/таможня/
  разгрузка), торговая шхуна/барка, вести о приходе судов.
- Разведки: Systems (водный Network, порт-функция, классы судов, знание, порядок),
  Economist (ёмкость/цена/скорость/фураж/пошлина). Водный этап — после сетки и шкалы времени.

## Цикл 54 — водный закон готов (ADR 0075, владелец)

- Главная находка Economist: **ёмкости судна нет вообще** — одна лодка везёт что угодно,
  людей без капа, фуража и пошлины нет. Это дыра, а не фича.
- Systems: реки как сети нет (связность/исток/устье/течение), порт = поселение у воды,
  склад = `Settlement.stores`, таможня через `Obligation` невозможна (двор, а груз анонимен),
  цена рынка и пошлина — отдельные правила.
- Решение владельца: ёмкость `raft` 6 / `boat` 12, люди 2/4; часы воды 0,17/0,14, вверх только
  `boat` ×1,5; река — сценарные данные `rivers` (связность проверяется); пошлина 10 % груза
  натурой `Ledger.transfer` в сток поселения (соль без пошлины); фураж только за ночёвку >6 ч;
  торговая барка/шхуна — отдельный ADR после (новый товар по И-7).
- Окна меряются и правятся открыто (two ≥ 14, shire 24–28 — гейт не трогаем).

## Цикл 55 (2026-09-24): срез принят, вшит; этап сетки открыт

- Table: **560 OK**, каноны побит (12/29, 14/341, 24/530), повтор шира — тот же хеш,
  дельта 0; общинный доступ, племя (independent → allied: оброк 0.6/двор, `arrears`, уход
  запрещён, пустое поселение → `open_field`), дорога 20 дн + 3 бревна за ≤1 мес, `marks:`
  из YAML, тропа видна только с холма. **ПРИНЯТО**, починок 0.
- Вшил (CHANGELOG). Решения по этапу сетки отданы Implementer'у: открытый сдвиг чисел новым
  ADR (окна §1 не трогаем, подгонка данных запрещена), топология живёт в одном хелпере
  соседей, чужие зоны — только импорт хелпера, каждый тронутый файл в отчёте.

## Цикл 56 (2026-09-24): sync доков сделан, дерево временно красное

- Scribe синхронизировал `02`/`03`/`04`/`05`/`09` под принятый срез: `Tribe` = 6 полей
  (факт кода), кап стройки 40, `common`/`grazing`, `open_field` для пустого поселения,
  `known_tiles`, формы домов/таверны как маркеры. Grep-трассировка подтверждена.
- **Дерево красное не от доков:** Implementer в середине среза гексов — падает
  `NameError: tile_id_at` в `engine/seat.py:81` (500 ошибок). Это freeze-срез, не регресс;
  зелёный прогон — после его сдачи, затем Critic → Table.

## Цикл 57 (2026-09-24): шестигранная сетка сдана, Critic принял, ждём Table

- Implementer сдал сетку: **569 OK**; `engine/hexgrid.py` — единственный источник соседства
  (6 направлений, offset↔axial, расстояние); `state_hash` покрывает координаты; 9 тестов,
  включая пин «нет локальных переборов соседей». Чужие зоны — только импорт хелпера
  (`decisions`, `labor`, `livestock`, `exchange`, `thegn`, `regimes`), формулы не тронуты.
- Перемер: hill **13**/15.6/**28**, two **17**/15.6/**384**, shire **24**/15.6/**548** — все
  в окнах §1; причина сдвига раскрыта в ADR (6 соседей → миграция/путь/доступ), подгонки нет.
- Critic: **ПРИНЯТО С ЗАМЕЧАНИЯМИ**. Замечания: пин-греп узкий (не блокер, взять в долг),
  коллизия номера ADR (мой 0072 занят) — Implementer переименовывает свой в **0076**.
- Table: приёмка запущена (каноны: hill 13/28, two 17/384, shire 24/548).
- Implementer сразу получил **этап 3: честное время** (ADR 0069/0070/0071) — часы пути,
  ETA по дням, дневной контур Pack.
- Economist сдал проект «правило места»: 5 гексов на 100 семей — крыши, еду даёт хлебный
  пояс (~30 гексов на 100 семей, гекс кормит ~3.3 семьи). Моё решение: кап 20 дворов на
  городской гекс (сельский 5), 40 — только с пригородом 2+ гексов; специализация v1 —
  оружейники/мельники/мясники через существующие `place=settlement` рецепты; бондари,
  каменщики, ювелиры, вода-как-ресурс, `House` — в v2 отдельными ADR (новые товары запрещены
  И-7). Контекстный кап — задача Implementer'у после этапа 3.
- Systems: проект «кольца знания при 6 соседях» (1 факт / 2 гекса слух / границы карты).
  Legal: проект «служба 60–90 суток» (существующими сущностями, без новой).
  World: спецификация карты 100×100 в гексах с хлебным поясом.
- Legal сдал проект службы; моё решение: один ADR «служба 60–90 суток» — `muster`-повинность
  с `period_months=2/3` (дедлайн от даты выхода), связь `Pack`↔повинность (туда+обратно,
  без новой сущности), `call_status` получает `in_service/returned/overdue`, `Pack.lost` ≠
  отказ, подавление тэна — образцом `revoke_thegn`; новых полей (регент, `annexed` для племени)
  не вводим — отдельный политический ADR позже. `service_bad_months` не переиспользуем как
  календарь экспедиции.
- Systems сдал проект последствий гексов. **Мои решения (правила, не допуски):**
  кольца знаний 1 / 6 / 12 (кольцо 2 — слух ~0,5, дальше тишина, `known_tiles` только из
  доставленного `Report` — И-3); `delay_days` — задержка **канала**, не время пути (2 гекса =
  0,65–1,3 ч ноги, не 1–2 суток); **соседство ≠ право** → ADR 0077 (Legal ведёт: доступ только
  `common`/`works_tiles`); миграция — только в пригодный гекс (не вода/руина/режим без жилья),
  локальный кап 5, глобального капа не заводим, «все шесть заняты — двор остаётся»
  (ADR 0078, Implementer после этапа 3). Допуски (не правила): вероятность потери кольца 2,
  длина маршрута/число партий как бюджет раскрытия карты (`3L+4` факты, `5L+14` с слухом,
  10 000 гексов ≈ от 20 партий при L=100), плотность на 100×100 меряем позже.

## Цикл 58 (2026-09-24): спецификация карты 100×100 принята как вход этапа 6

- World сдал спецификацию (правок нет): река ~198 водных гексов (русло 116 + 2 притока),
  3 брода, 2 моста, порт в устье; холм/город (45,49–51) с 8 квартальными гексами; 8 обычных
  деревень по 50 семей на 10 гексах (5/гекс) + племенная + соляная; хлебный пояс города ~30
  гексов, деревням по ~15; 3 лесных массива по 300–500; wilds — остаток; редкие места только
  данные/markers, случайного спавна из тика нет.
- **Мои решения:** старые три сценария остаются регрессионными мирами (36/60/60 — горизонты
  времени, не размер карты); новая карта = отдельный сценарий со своим честно измеренным
  baseline, подгонки под старые числа запрещены. Порт — `Settlement` у воды + marker, без
  сущности `Port`. **Соль — сухое засолённое место в юго-восточном бассейне у реки (не на
  воде)**, дорога к порту: соль и порт получают настоящий смысл. Посевной резерв — число не
  выводится (нет товара `seed`): еда считается из `monthly_food_need`, а не новой константой;
  посевной резерв — отдельным ADR Economist'а.
- **Код, который потребуется на этапе 6 (очередью за этапом 3):** загрузчик размещения дворов
  по гексам (`at`/раскладка) — сейчас все дворы сажаются в `Settlement.coord`; ленивый coarse
  рост вместо обхода всех 10 000 гексов; проверка связности реки/истока/устья/притоков; водный
  доступ и ёмкость по ADR 0075.

## Цикл 59 (2026-09-24): ADR 0077 «соседство не даёт прав» — код сдан, приёмка идёт

- Legal: доступ к общему сбору только `works_tiles`/`Right.common`; `is_neighbor` и `tenure`
  больше не дают communal-доступ, `own_tiles` не тронут; профильные тесты 10/10; соль A1
  честно 6.45 → **7.33**. Каноны: hill **13**/15.6/**28**, two **17**/15.6/**383**,
  shire **25**/15.6/**535**; дельты 0. Окна §1 не пробиты (живые 14–18 и 24–28, соль 15.6);
  третье число (голод) — показатель без окна, сдвиг честный.
- Разъяснено владельцем: СТОП-условие — это окна на живых и соли, не на метрику голода.
  Legal дополняет ADR 0077 и гоняет полный сюит; Critic и Table принимают срез.
- Critic по 0077: **ПРИНЯТО С ЗАМЕЧАНИЯМИ**. Закон держится (сосед → False, `common` → True,
  `tenure` → False; `own_tiles` цел; обходов доступа в `labor`/`livestock` нет; дертиминизм,
  дельта 0; новых сущностей нет). Замечания: (1) докстринги в `economy/labor.py:251` и
  подобные места ещё обещают доступ по соседству — Economist чинит свою зону; (2) расхождение
  с прошлой сдачей (two 384→383, shire 548→535) — ожидаемо, 0077 сузил доступ.
- **Моё решение:** отдельную приёмку 0077 не гоняем — Implementer уже переводит путь на часы
  (этап 3), а на движущемся дереве каноны бессмысленны. 0077 + этап 3 принимаются **одним
  циклом** (Table), потом вшивание и Scribe. 0077 как правило финален.
- Economist вычистил ложные докстринги своей зоны (`labor.py:251`, `livestock.py:202/471/536`):
  доступ только `works_tiles`/`Right.common`; соседский торг (ADR 0057) и восприятие тэна —
  отдельные механики, геометрия там законна (ADR 0077 п.2); профильные тесты 10/10.

## Цикл 60 (2026-09-24): ПРОТИВ на freeze — протокол приёмки ужесточён

- Table (вне моей команды) прогнал приёмку на живом дереве и вынес **ПРОТИВ**: 12 красных,
  каноны сняты на меняющихся файлах. Причина — **моя ошибка диспетчеризации**: приёмка
  пошла параллельно с работой Implementer'а и правками Legal'а (ADR 0038 §4 это запрещает).
- **Новый протокол (закон для студии):** приёмка запускается **только** после явного handoff
  автора «среда зелёная, freeze». До этого ни Table, ни Critic не гоняют приёмочные прогоны;
  после freeze авторы не правят дерево до вердикта.
- 12 красных отданы Implementer'у (его зона): `test_march_path` ×3, `test_caravan_module`,
  `test_caravan_trade`, `test_caravan_report`, `test_cycle_road_river_roi`,
  `test_draft_livestock`, `test_hay_before_field` ×2, `test_bloom_and_hay` ×2. Пины — на
  измеренные часы; инварианты тестами: дорога дешевит, но не до нуля; плот выигрывает только
  с водой на маршруте; сено до пашни; выпас 5–9 ограничен.
- Проверки, не сломавшиеся: 6 соседей у гекса, брод снимает обход, тропа/дорога удешевляют,
  миграция по шести соседям, окна держат.

## Цикл 61 (2026-09-24): этап «честное время» сдан под freeze, приёмка запущена

- Implementer: `travel_hours` (нога 0,65 / конь 0,40 / обоз 0,85 ч/гекс, `arms` ×1,25),
  `entry_hours = entry_cost × hours_scale`, `eta_date` по суткам (месяц = 30 суток), дневной
  контур `phase_day` только для `Pack` (И-4 цел); 12 пинов перенесены на измеренные часы,
  новый `test_travel_hours.py` (20 тестов). Сюит **591 OK** (дважды).
- Каноны: hill **13**/15.6/**28**, two **17**/15.6/**383**, shire **25**/15.6/**535** — окна §1
  целы; дельта 0; детерминизм повтором. ADR `0078_honest_travel_time.md`.
- Чужая зона: `economy/caravan.py` — только перевод единиц, формулы не тронуты.
- Вопрос на ревью (мой): Implementer **удалил пин** в `test_caravan_report` («ненулевой образец»)
  и сослался на честный ноль при приходе в том же месяце — Critic проверяет, что инвариант
  остался содержательным. Плот 3,74 ч против 5,1 ч суши: в часах быстрее, в сутках разница
  не видна — инвариант «плот только с водой» закреплён.
- **Нумерация:** ADR 0078 занята временем, правило кандидата миграции становится **0079**.

## Цикл 62 (2026-09-24): дыра `salt_channel_vacuous` — моё решение: канал не декоративный

- Critic по времени: **ПРИНЯТО С ЗАМЕЧАНИЯМИ**; механика часов честна (И-4 цел, скрытых
  множителей нет, 42/42 профильных, дельты/детерминизм ок). Замечание существенное: в живом
  тике **все 39 соляных отчётов — нули**. Причина реальна: обоз с коротким путём приходит в том
  же месяце и увозит соль из деревни **до** `phase_inform`, поэтому отчёт честно пуст. Удалённый
  пин был единственным детектором этого — теперь регрессия «отчёт всегда ноль» непроверяема.
- **Моё решение:** это дефект, а не украшение. Канал «игрок знает о соли» — ядро петли, он не
  может быть декоративным. Настоящий отчёт — это **события месяца** (пришло/увезено/выросло),
  а не снимок склада после фаз; ложный пин «ненулевой образец» не возвращаем, вместо него
  честный детектор: «если обоз привёз соль в деревню — отчёт ненулевой и называет это; если
  за месяц ничего не было — ноль законен». Правка: события/порядок фаз — Implementer
  (`engine/tick.py`, тесты), слово/доставка вести — Info (`news/`, `Report.delay_days`).
  Это правило, не починка числа: **ADR 0079 «отчёт — события месяца, не снимок»**.
- Переименование: правило кандидата миграции (из цикла 57) становится **ADR 0080**.
- Готовлю срез «честное время» к вшиванию после вердикта Table; починенный соляной канал —
  следующим срезом, не в этом.

## Цикл 63 (2026-09-24): срез принят и вшит; новый Info заведён по разрешению хозяина

- Table: **ПРИНЯТО**, 591 OK, freeze соблюдён (последняя правка 19:18:44, приёмка 19:29–19:48,
  новых файлов нет), починок 0. Каноны совпали с ADR 0078 до цифры; глазами путника и
  крестьянина — зелёно. CHANGELOG дополнен записью.
- Implementer: чинит соляной канал (движок отдаёт события месяца, честный детектор вместо
  удалённого пина). Scribe: синхронизация доков под принятый срез.
- Старый чат Info (архив) закрыт; **хозяин разрешил заввести нового Info** — новый агент
  `b92fda2f` (зона `news/`, ADR 0079: отчёт = события месяца, `delay_days` — канал, И-3).
  Это первое явное исключение из правила «новых субагентов нет» — с разрешения хозяина.

## Цикл 64 (2026-09-24): хозяин снял запрет на новых субагентов — параллелю по зонам

- **Решение хозяина:** «создавай любых субагентов, каких требуется», модель только
  `opencode-go/space-bunny-free`. Ограничение «не заводить новых агентов» больше не действует.
- Новый **Implementer-2** `46df6afc`: карта 100×100 — скелет, река (116 + 41 + 41 гексов воды,
  3 брода, 2 моста, порт в устье), города/кварталы, 8 деревень по 50 семей на 10 гексах,
  племенная и соляная, лес/wilds, **загрузчик раскладки дворов по гексам** (сейчас все дворы в
  одной координате), проверка связности реки, **ленивый coarse-рост** (10 000 гексов не
  обходятся каждый тик). Зона: `design/scenarios/`, `scenario.py`, `world/`, его тесты.
- Сессия старого Implementer'а зависла (ходы без действий, обрыв финальных сообщений) —
  перезапущен внутри роли; срез ADR 0079 (события месяца) продолжает он.
- Economist: **вода v1 по ADR 0075** — ёмкость 6/12, фураж на день от часов (1,75/2,75/4,5),
  портовый сбор 10 % натурой; вода-как-ресурс и шхуна вне v1.
- World: **вид карты 100×100** — маркеры брода/моста/порта/соляного/шахты/редких мест, различимый
  вид квартала и деревни, река/лес/wilds как coarse-вид без выдачи знания (И-3).
- Новая Info `b92fda2f`: вести и доставка по ADR 0079. Scribe `d84d7a32`: sync доков.
- Параллельность только по **непересекающимся зонам**; приёмка — после freeze, одним циклом.
- Развилки Implementer-2 решены: (1) **река — эталон точные координаты, числа пересчитываются**
  (мои 116/41/41 арифметически неверны, виноват владелец; подгонки запрещены); (2) **кварталы:
  100 дворов = 5 занятых гексов по 20** (кап города 20), 3 гекса — резерв под городские работы
  без дворов (12–13/160 отклонены); (3) **coarse-рост: природный рост не отключаем** — новый
  модуль `engine/growth.py` (детально только по `works_tiles`, wilds/лес статичны) пишется
  сейчас, **правка `engine/tick.py` — одной строкой и только после freeze** первого
  Implementer'а (он в `phase_inform`); (4) YAML-координаты остаются `[col,row]`, связность
  проверяется по реальным 6-соседям, `offset_to_axial` не трогаем (ADR 0076 запинен), невязь
  вертикального прогона лечится **перерисовкой пути**, не сетки.
- **ADR 0079** написан владельцем (`0079_report_is_month_events.md`): отчёт = события месяца,
  атрибуция по владельцу стока (дворы), отток виден, честный событийный детектор вместо
  удалённого пина, И-3 цел. **ADR 0080** написан владельцем
  (`0080_migration_candidate_eligibility.md`): кандидат миграции — только пригодный гекс
  (не вода/руина/режим без жилья), шесть заняты — двор остаётся, глобальный кап плотности
  не заводим.
- Сессия Implementer'а (7144a7f4) зависла окончательно (оборванные ходы, `NameError:
  trail_level_for` в `economy/caravan.py:334` оставил) — **припаркован**; его половину среза
  забрал новый **Implementer-3** `392be82a` (довести соль до зелёного, интеграция, ADR от 0081,
  handoff короткий). Info `b92fda2f` сдал свою половину (`news/month_events.py`,
  `test_news_month_events.py`, обновлённые `test_caravan_report*`); его файлы не откатывать.
- Scribe `d84d7a32` синхронизировал `docs/04_tick.md`, `docs/07_legal.md`, `docs/09_economy.md`
  под принятое (часы/сутки/ETA/`phase_day`, соседство без прав, дыра соли как «ADR 0079, в
  работе», каноны 13/28, 17/383, 25/535; прежние числа не откачены).
- **Болезнь сессий:** три агента (Implementer 7144a7f4, World 9d441697) начали зацикливаться —
  финальные ответы обрываются на первой строке или повторяют старый отчёт, ходы без действий.
  Реакция: сломанные сессии **паркуются**, работа передаётся свежему агенту с чтением текущего
  состояния файлов (Implementer-3 `392be82a`, World-2 `260c57a6`). Правило для студии:
  финальный handoff — **короткий (≤10 строк)**, детали в docstring/ADR.
- **Живой состав сейчас:** Implementer-2 `46df6afc` (карта 100×100: сценарий, раскладка
  дворов, модуль coarse-роста), Implementer-3 `392be82a` (соль/ADR 0079, починка дерева),
  Economist `a074e263` (вода v1: ёмкости, фураж на день, пошлина 10 %), Info `b92fda2f`
  (новый; вести по 0079, сдал), World-2 `260c57a6` (новый; вид карты), Scribe `d84d7a32` (доки),
  Critic `7a743124`, Table `57dea483` — ждут freeze.
- **Коллизии зон (моя вина, развёл):** дерево было красное из-за двух чужих незавершённых
  правок — `economy/caravan.py:334` (`NameError: trail_level_for`, чинит Economist) и
  `scenario.py:501` (`quarter_tiles=None` в `_point_many`, чинит Implementer-2). Implementer-3
  соль не трогал и ждал развода; после их правок он продолжает.
- **Legal `5cee7dd5` получилADR 0066 + службу 60–90 суток** (повинность `muster` 2/3 мес,
  outbound/return Pack, `call_status in_service/returned/overdue`, `lost` ≠ отказ, подавление
  тэна образцом `revoke_thegn`, регент и `annexed` — вне v1). Зона: `legal/`,
  `obligations.yml`/`rights.yml` — не пересекается с водой v1 Economist'а.
- Legal'у дано **именное исключение** на `ontology.py` (минимально: `Obligation.call_status`
  с закрытым списком + один идентификатор связи с Pack). `world.py`/`state_hash`/`engine/` —
  запрещены (заняты Implementer-3); **покрытие нового поля в `state_hash` — обязательный
  follow-up**, который выдам владельцу движка после freeze. Смена хешей канонов —
  открытый сдвиг, числа не меняются.
- **World-2 `260c57a6` сдал вид:** `engine/tile_view.py` — coarse-карта 100×100
  (`CoarseMapView`, `coarse_map_view`, дамп), только чтение данных; формы уже покрыты
  (`crossing`, `pier`, `salt_settlement`, `iron_mine`, `ruin_site`, `hermitage`, `waystation`,
  `smoke_site`, `lost_caravan`, `city_quarter`/`village`/`large_village`); профильные тесты 3/3,
  `compileall` OK. **Долг на приёмку:** `test_land_vision` видит 2 старых несовпадения сценария —
  владелец не менял; разобрать на общей приёмке (вероятно, след загрузчика Implementer-2).

## Цикл 65 (2026-09-24): три параллельные правки оставили дерево красным — окно починки

- Мой прогон: `Ran 617 tests`, **FAILED (failures=27, errors=2)**; Legal подтвердил: **625 тестов,
  27 FAILED, 2 ERROR**. Красное не «старое» — оно от трёх параллельных незавершённых срезов.
- **ПровереноEconomist'ом, ложная тревога И-1:** `test_salt_exempt` считал `delta=8.0` из
  **собственной фикстуры** теста (8.0 соли вписаны прямой записью в сток без
  `capture_initial`); production-код (`water.py`, `port_toll`) — **transfer-only**, материя не
  создаётся. Починка: `capture_initial` в тесте; `test_water_trade` 9/9, дельта 0. И-1 в коде
  цел, мой прежний вывод «портовый сбор создаёт материю» **неверен**.
- По виду: `test_tile_view.TestTileForms.test_root_seat_is_hall` — `heath` вместо `hill`.
  Владелец: World-2 (только отображение данных, не подгонка константы).
- По загрузчику: `test_communal_access` (A1 «соль 4.0») и 2 расхождения `test_land_vision`.
  Владелец: Implementer-2 (старые сценарии — регрессия, их числа не меняются).
- Соль (ADR 0079) — Implementer-3 ждёт зелёного дерева; вызов/служба (ADR 0082) — Legal сдал
  (`ontology.py`, `legal/service.py`, `test_muster_service.py`, 24/24, дельта 0; связь
  `Pack.obligation_id`, outbound/return, `period_months=2/3`, `call_status`; `world.py/state_hash`
  не тронуты, покрытие новых полей — follow-up).
- Зацикленный World (9d441697) **архивирован** — он мог портить `tile_view.py` поверх World-2.
- **Моё решение по параллельности:** дальше авторы работают **по одному файлу за раз**, каждый
  чинит и сдаёт короткий handoff; общий прогон — только у Table после freeze. Три параллельных
  автора на одном дереве дали три коллизии и 29 красных — это моя ошибка диспетчеризации.
- **Карта 100×100 сдана (Implementer-2):** `design/scenarios/v0_barony_100.yml` — 10 000 гексов;
  река 6-связна (потоки 126/41/41, уникальных 199, с прудом 200), 3 брода, 2 моста, порт-поселение;
  12 поселений, 600 дворов (город 100 = 5×20 + 3 резервных квартала; 8 деревень 8×50 = 10×5;
  племенная и соляная по 50; шахта — marker); леса 400/360/480, лугов 115, wilds/heath 8 283;
  редкие места только данными. Раскладка дворов по гексам в загрузчике; индекс роста
  `engine/growth.py`. **Замер: 30 вызовов роста — 192 гекса 0.045 с, 10 000 гексов 0.161 с**
  (52× гексов = 3.5× времени, обход всей карты не каждый тик). 12 месяцев: 550 живых/50 ушли,
  дельта 0, хеш `3f8d173ce5d7`, повтор детерминирован; 44 профильных теста; старые каноны
  целы (13/28, 17/383, 25/535). Подключение `growth.py` в `tick.py` **отложено** — файл занят
  `phase_inform` (это правильное решение, не долг).
- **Осталось красное (3 пина обозов):** `test_caravan_module` ×3,
  `test_caravan_trade.test_road_on_route_shortens_days_and_keeps_delta` — Economist, честный
  перенос пинов, инварианты не ослабляем.
- **Очередь после соли:** подключить `growth.py` в `tick.py` (одной строкой, разрешаю) и
  исполнить **ADR 0080** (кандидат миграции — пригодный гекс) тем же владельцем движка.

## Цикл 66 (2026-09-24): FREEZE — дерево зелёное, пакет из пяти срезов на приёмке

- Мой прогон: `bash sim/run_tests.sh` → **`Ran 636 tests … OK`** (332 с). Freeze настоящий.
- Готовы к приёмке: (A) соль/события месяца ADR 0079 + 0081 (целевые 9/9, событийные 9/9,
  каноны 13/28, 17/383, 25/535, дельта 0, повтор хешей); (B) вода v1 ADR 0075 (24/24, дельта 0,
  transfer-only, `capture_initial` — честная правка фикстуры); (C) карта 100×100 (44 профильных,
  550 живых/50 ушли за 12 мес, дельта 0, хеш `3f8d173ce5d7`); (D) вид (coarse-карта, только
  чтение данных); (E) служба/вызов ADR 0082 (24/24 профильных, дельта 0).
- Critic и Table запущены на одном замороженном дереве; отдельно проверяется **ненулевое
  число соляных отчётов в живых прогонах** (было 0/39 — теперь это ПРОТИВ, если снова ноль) и
  severity долга: `state_hash` не покрывает новые поля `Obligation`.
- **Два долга, записанных честно:** (1) `state_hash` не покрывает `call_status`/связь Pack —
  риск одинакового хеша у разных состояний; (2) `can_settle`/`tile_max_households` живут в
  `tile_view.py` — механика в зоне вида (нарушение карты зон), не блокер, но фиксирую.
- **Очередь следующего среза:** подключить `engine/growth.py` в `tick.py` (одной строкой),
  исполнить **ADR 0080** (пригодный гекс) и написать **ADR 0083** — механический кап
  (сельский 5, городской 20 по маркеру `urban`): сейчас «кап 20» живёт только как раскладка
  данных, а `can_settle` для городского гекса вернёт ложь (20 ≥ 5).

## Цикл 67 (2026-09-24): Table — ПРИНЯТО по пакету из пяти срезов

- `Ran 636 tests` **OK**, правок кода 0, freeze соблюдён (последняя правка 20:46:01, приёмка
  20:49–21:23).
- **Соль (0079/0081) ПРИНЯТО:** ненулевые соляные отчёты — hill **4 отправлено / 4 доставлено**,
  two **11 / 11**, shire **10 / 10**; провала 0/39 нет, игрок видит уход и приход
  (`вёз 2.13, доехало 2.13`), `salt_approx`/`salt_received` не пустые.
- **Вода (0075) ПРИНЯТО:** 9/9; плот 6/лодка 12, перегруз отвергнута, люди не бесплатны и не едут
  в грузу; фураж по долям суток, короткий рейс без фуража; пошлина 10 % натурой, соль exempt,
  `delta=0`; вода без переправы непроходима.
- **Карта (ПРИНЯТО):** 10 000 гексов, 12 поселений, 600 дворов; загрузка 2.9 с, 12 тиков 1.2–7.2 с
  (суммарно ~32 с), `delta −0.0`, повтор — тот же хеш `3f8d173ce5d7`; рост O(поселения),
  линейного взрыва по 10 000 клеток нет; `test_barony_100` 11/11.
- **Служба (0082) ПРИНЯТО:** 8/8; срок 2/3 мес, дедлайн от outbound, outbound↔return одной
  повинностью, `overdue` ≠ `refused`, гибель = `unable`, тишина ≠ отказ, подавление тэна через
  `revoke_thegn`. Долг (не ПРОТИВ): новые поля повинности не покрыты `state_hash`.
- **Каноны ПРИНЯТО:** hill 13/15.6/28 `79796030ead0`; two 17/15.6/383 `7fddcf96722c`;
  shire 25/15.6/535 `73f56b9133d6`; соль 15.6; дельта 0; повтор шира — тот же хеш.

## Дорожная карта этапов (владелец, 2026-09-24)
Порядок обязателен: каждый шаг — приёмка Critic → Table → вшивание → Scribe.

1. **Принять накопленный срез** (Table; чат студии перезапустить) — тропы, `common`,
   племя v1, оброк, формы домов, `marks:`, допуск капа 40.
2. **Сетка** (ADR 0071): квадрат → шестиугольник, только топология, без правил.
3. **Время** (ADR 0069/0070): `travel_hours`, `eta_date` по дням, дневной контур `Pack`,
   `delay_days`; фураж и каденция под новые часы.
4. **Место** (ADR 0072): деревня ≤5 дворов, квартал 20–40 семей, мастерские рецептами.
5. **Разведка** (ADR 0073): правила наблюдения, разведочная партия, находки, гекс известен
   только из вести; wilds и редкие места.
6. **Карта 100×100** (ADR 0073): река (100–120 гексов + 2 притока), 2–3 брода, 1–2 моста,
   порт в устье, 3 лесных массива (300–500 гексов), 8–10 деревень (поля 5–15, луга 2–5),
   соляной гекс, железная точка, руина; остальное — coarse-данные, не агенты.
   Перенос трёх канонов в новую карту — после сетки.
7. **v2 племени** (ADR 0066): действие вызова, отказ/неявка, вести.
8. **Служба 60–90 суток** — отдельный ADR (срок службы тэна и племенной вызов).
9. **Экспансия за карту** (соседние баронства) — отдельный ADR.
10. **Города 100–200 семей** — после 4 (кварталы) и реальной потребности.

Не трогаем без отдельного ADR: округа 6–8 баронств (агрегат), тактический бой, гилдии,
рынок рабов, присяга, гекс колонны до v2 вызова.
