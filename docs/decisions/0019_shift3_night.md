# 0019. Итог смены 3: J1–J4 закрыты, канон смены 3 снят (Scribe)

## Контекст

Смена 3, 21.09.2026, тот же репозиторий/чат. До патчей SimRunner снял baseline (J5):
**217 OK**; `v0_hill_and_salt` 36/1729 — соль **15.6**, `delta -0.000000`, hash
`17613abecd0ccde6d6b8bcea884ac72b0ccaaf79af6845fd767968127e27ab39`; `v0_two_settlements`
60/42 — живых **17**, голод **272**, hash
`affee56b3a429c9a424d70a9657188c8a79c1e21bb2f295967c0b9f8e9a2a7d3`; `v0_shire` 60/1729 —
живых **26**, голод **535**, hash
`abf199ef25df81bf1325d2ef2cb7a57258da08ed3e158552243e35b1b5de969a`; unload соли **11.6**
везде. Ворота смены: J1 жив → общину «поровну» не трогать и поселения не добавлять;
J1+J2 закрыты → J3–J4; третий тен / 50 дворов запрещены. ADR 0001–0018 не редактировались,
`design/scenarios/HASHES.txt` — история смены 2 и не переписывается.

## Что закрыто

**J1. Корневой держатель (Legal).** `can_leave` ложен для держателя ЛЮБОГО манора (корень
+ вложенные): голод и недоимка не уводят `hh_court` (`left_at None`, новых `Pack` 0); `tied`
не уходит; свободный `hh_06` уходит; I3 цел (тэны фьеф не бросают). Critic 0017: J1 закрыт,
I3 цел, канон смены 2 заморожен; STOP нет.

**J2. Канон (Scribe).** `design/scenarios/ACCEPTANCE.md` §6 «Ревизия канона» +
`design/scenarios/HASHES.txt` (история, не переписывается); окна 14–18/24–28 привязаны к
ревизии смены 2.

**J3. Община (Legal+Economist).** `waste`/`reserved_wood` — ПРАВО ДОСТУПА и общий кап
клетки (4) на сбор, а не «каждый двор = полная виргата»; чужой двор доступа не имеет;
держимая пашня как G3; REJECT-вариант, ронявший соль, не возвращён. Числа: два двора на
одной общинной клетке — **4** партии (не 8); пашня держимой клетки — **4/8**; seed 99 соль
**6.074**, `delta≈0`. Critic 0018: J3 закрыт.

**J4. Второй тен (Implementer).** `grant` M18 забирает из корня ещё **1** доменное поле
(`t_06_02`) и **1** двор (`hh_08`); у второго тэна 3 клетки, **2** домена, 3 двора; голод
держателя **39→30** мес (< 39), финальный `hunger_days` 0, `mustered True` 13 мес; корневой
`corvee` в жатву **240→204**. Цена честная: первый тэн просел **30→33**, корень потерял
последнее доменное поле и двор. Yield/каталоги не тронуты. Critic 0018: J4 закрыт.

## Что осталось ложью/ограничениями

1. Шире мир — голоднее корень: shire глобальный голод **535→568** (цена J4), живых 27;
   первый тэн честно просел (33 мес).
2. Корневой держатель теперь не уходит (J1), но остаётся голодным в бедные месяцы (в shire
   60 мес финальный `hunger` 29) — честный голод, не баг; пустого корневого манора больше нет.
3. Сбор с общинных клеток шарится; ПАШНЯ общинных полей по-прежнему нет (правило: делится
   только держимая клетка) — это законный режим, не дыра.
4. Поломка телеги естественным 60-месячным прогоном не показана (зонд H1) — открыто с 0016.
5. Приёмка по-прежнему возможна только на замороженном воркспейсе; канонический лист —
   `ACCEPTANCE.md` §1/§6.
6. Слот смены 3 в `ACCEPTANCE.md` был черновым (`555`/`788e6d2e`) — обновляется этой записью
   (не подгонкой прогона).

## Числа стресса (runner, строго последовательно)

| мир | живых | соль | голод | delta | hash (полный/префикс) |
|---|---|---|---|---|---|
| `v0_hill_and_salt` 36/1729 | 12 | 15.6 | 33 | -0.000000 | `17613abe…` (не менялся смену) |
| `v0_two_settlements` 60/42 | 18 | 15.6 | 289 | -0.000000 | `09c25081bbdc37a2daf8c2a01b10799cfe30ef4f388fbda4d8a87fa2c1ae3b4e` |
| `v0_shire` 60/1729 | 27 | 15.6 | 568 | 0.000000 | `3500373dd30fdb47c2d412fe629a506a1af0c1f6f7076035cdfdc5371fd5df77` |

Replay: два прогона одного сида — одинаковый hash (two/42 и shire/1729). Соль: load 7 /
unload 5 / **11.6** / `external_in 0` / телепорт 0. Тэны: **2** (`grants=2`), третий отклонён.
**230 тестов OK.**

## Альтернативы

- Оставить корневого держателя «как свободного» — отклонено J1 (амбар без хозяина).
- Община «поровну» (каждому полная виргата) — отклонено: кап клетки.
- Поднять yield, чтобы второй тен сытел — запрещено; выбрано одно разрешённое изменение
  (пакет из корня).
- Канон без хешей/ревизии — отклонено J2 (память врёт).

## Последствия

- J1–J4 закрыты, канон смены 3 снят и зафиксирован в `ACCEPTANCE.md` §6: 230 OK, соль 15.6,
  delta ±0.000000, живых 18 (two) / 27 (shire).
- Цена J4 честно записана: корень и первый тэн просели по голоду, shire вырос до 568.
- Открытые пункты 1–6 остаются; каждый — отдельная задача с новым ADR.
- Смена 3 — новый baseline; при изменении экономики окно меняет новый ADR, а не подгонка.

## Что запрещено следующим агентам

1. Ломать J1 (корневой holder), I3 (фьеф держит тэна), J3 (общий кап общины), A1 (соль)
   без нового ADR.
2. Возвращать «каждый двор = полная виргата» на общинных клетках и REJECT-вариант G3 для
   пашни.
3. Поднимать yield / создавать материю / кормить голод «богом», маскировать наймом корня.
4. Третий тен, 50 дворов / 4 тэна, биржа / глобальные цены, кузня / кожа / бой / Godot / LLM.
5. Редактировать ADR 0001–0018 и `design/scenarios/HASHES.txt`.

## Проверка

```bash
bash sim/run_tests.sh
PYTHONPATH=sim/src python3 -m hillcourt.runner --scenario design/scenarios/v0_hill_and_salt.yml --months 36 --seed 1729
PYTHONPATH=sim/src python3 -m hillcourt.runner --scenario design/scenarios/v0_two_settlements.yml --months 60 --seed 42
PYTHONPATH=sim/src python3 -m hillcourt.runner --scenario design/scenarios/v0_shire.yml --months 60 --seed 1729
```

Критерий: `Ran 230 tests ... OK`; `v0_hill_and_salt` 36/1729 — соль **15.6**, `delta
-0.000000`, hash `17613abe…`; `v0_two_settlements` 60/42 — живых 18, голод 289, соль 15.6,
`delta -0.000000`, hash `09c25081…`; `v0_shire` 60/1729 — живых 27, голод 568, соль 15.6,
`delta 0.000000`, hash `3500373d…`. Окна смены 3 — в `design/scenarios/ACCEPTANCE.md` §6.
Git status до/после записи ADR идентичен (` M README.md`, далее untracked `AGENTS.md client/
design/ docs/ sim/ tools/`).
